# حالة تنفيذ النظام المحاسبي متعدد الشركات

آخر تحديث: 27 أغسطس 2026

## ملخص تنفيذي

- البنية: Modular Monolith داخل npm workspaces.
- الخادم: Express وTypeScript وPrisma 7.9.1.
- الواجهة: React وVite بواجهة عربية وإنجليزية وأردية وهندية تشغيلية تشمل أرصدة المخزون وحركاته والتسوية البنكية إضافة إلى الوحدات السابقة، مع RTL/LTR والتجاوب.
- قاعدة التطوير: MariaDB 10.4 عبر XAMPP.
- الترحيلات الحالية: 45؛ يضيف الترحيل 45 تشغيلات الإقفال المالي ولقطات قائمة الجاهزية وحزمة الإقفال وبصماتها وعلاقات الممثلين ومستند نتيجة السنة. يلزم إعادة بوابتي MariaDB 10.11 وMySQL 8.4 قبل أي نشر.
- حزمة التحقق الأخيرة للمرحلة 69: 273 اختبار API كاملًا من قاعدة فارغة، و28 للواجهة، و34 للبنية التحتية، و80 حالة Playwright للمصفوفة المتجاوبة، ورحلتا E2E. طبقت القاعدة المعزولة جميع الترحيلات الـ45 والـSeed ثم نجحت `273/273` قبل حذف قاعدة الاختبار المحددة.
- الحالة: نُفذت المراحل 61–69 محليًا وتنتظر بوابات الإصدار وأمر نشر صريح؛ اكتملت واجهة المطابقة البنكية والإطلاق التدريجي، ثم دورة الإقفال المالي المراجع وقيد نتيجة السنة وعكسه. آخر مرحلة منشورة هي المرحلة 60 عبر [GitHub Actions](https://github.com/ALMutasemObad/multi-company-accounting/actions/runs/32839845689) على الالتزام `c39b979`، ثم نجح [قبول التعافي الإنتاجي الفعلي](https://github.com/ALMutasemObad/multi-company-accounting/actions/runs/32840949844) و[فحص المراقبة التالي](https://github.com/ALMutasemObad/multi-company-accounting/actions/runs/32841244870).
- الحوكمة المعمارية: اعتُمدت ضوابط DDD وحدود الـBounded Contexts وADR-003؛ نُفذ `PostingEngine` وعقد عناصر الذمم والـTransactional Outbox، وحُسمت ملكية `Tax` و`Treasury` ودفتر الكميات في `Inventory`. تملك Treasury الآن بيانات المطابقة البنكية، وتقرأ Ledger عبر Query Port فقط.

## الحوكمة المعمارية — موثقة وقيد التنفيذ المرحلي

- [x] توثيق Architecture Audit للـDDD وEvent-Driven مع الأدلة ونقاط الترابط.
- [x] اعتماد خريطة مستهدفة لملكية الـBounded Contexts والجداول.
- [x] اعتماد ضوابط تحدد ما يبقى كما هو وما يجب إصلاحه وما يمنع مستقبلًا.
- [x] اعتماد ADR-003: يبقى Ledger متزامنًا، وتستخدم Transactional Outbox للآثار اللاحقة للـcommit.
- [x] إضافة قائمة مراجعة معمارية وتعليمات `AGENTS.md` للوكلاء والمطورين.
- [x] توثيق سياسة التزامن والـDeadlock والـDeadlines وترتيب الأقفال ومصفوفة الاختبارات المطلوبة.
- [x] اعتماد سياسة إلزامية لتوليد الرموز المرجعية غير الدلالية وثباتها وسلامتها تحت التزامن، وسياسة مستقلة لاستعادة كلمة المرور.
- [x] تنفيذ `PostingEngine` مركزي ومنع الكتابة المباشرة الجديدة إلى Ledger من بقية السياقات.
- [x] حسم Tax ownership في الكود وإزالة حاسبات الضرائب وCRUD المكررة.
- [x] حسم Treasury ownership في الكود وإزالة التكرار المتبقي.
- [x] بدء Inventory كسياق مستقل يملك `Warehouse` ثم `UnitOfMeasure` و`InventoryItem`، مع توثيق حدود المرحلة وخريطة توسع تمنع الكتابة المباشرة في Ledger.
- [x] تنفيذ دفتر حركة Inventory الكمي وإسقاط الأرصدة مع Idempotency ومنع المخزون السالب وترتيب أقفال موثق، من دون افتراض تقييم أو كتابة في Ledger.
- [x] ربط ترحيل وعكس فواتير الأصناف بدفتر الكميات عبر `InventoryInvoiceStockPort` داخل المعاملة المالية نفسها، مع تفرد المصدر والفشل الذري، من دون تقييم أو إشعارات محاسبية مولدة.
- [x] إضافة Outbox migration وworker لبريد التسجيل واستعادة كلمة المرور.
- [x] استبدال عقود التسوية المبنية على `targetJournalLineId` بـ`ReceivableItem/PayableItem` مملوكين لـAR/AP.
- [x] إنشاء Transaction Executor مركزي بretry classification وbackoff+jitter وdeadline موحد.
- [x] ضبط HTTP/request وPrisma transaction deadlines وإضافة metrics للـdeadlock والـretry exhaustion.
- [x] استكمال اختبارات post-vs-close وreverse-vs-settlement والـover-allocation المتزامن.
- [x] اعتماد OpenAPI عقدًا تنفيذيًا موحدًا لكل أجسام JSON مع توليد حتمي وحراس طلبات وفحص استجابات وبوابات تمنع الازدواج والتقادم.

المراجع: [فهرس الحوكمة](architecture/README.md) و[تقرير التدقيق](ARCHITECTURE_AUDIT_DDD_EVENT_DRIVEN_AR.md).

## المرحلة 1 — الأساس

- [x] إنشاء المستودع الموحد وتطبيق Express وواجهة `/health`.
- [x] إنشاء تطبيق React عربي أولي.
- [x] إضافة عقد OpenAPI المرجعي.
- [x] إنشاء نماذج المؤسسة والشركة والمستخدم وإسناد المستخدم للشركة.
- [x] إنشاء مخزن Idempotency دائم.
- [x] إنشاء إعدادات البيئة وDocker Compose وخيار XAMPP المحلي.

## المرحلة 2 — المصادقة وسياق الشركة

- [x] جلسات `PRE_AUTH` و`AUTHENTICATED`.
- [x] CSRF للطلبات المعدلة للحالة.
- [x] Argon2id لكلمات المرور وتدوير معرف الجلسة عند الدخول.
- [x] قفل الحساب بعد المحاولات الفاشلة.
- [x] عرض الشركات المسندة واختيار سياق شركة فعال.
- [x] منع الوصول المتقاطع بين الشركات.
- [x] تسجيل الخروج وعرض الجلسات وإلغاؤها.

## المرحلة 3 — الأدوار والصلاحيات

- [x] نماذج الأدوار والصلاحيات وإسنادها داخل الشركة.
- [x] فرض الصلاحية المطلوبة لكل مسار.
- [x] Seed متكرر التشغيل للصلاحيات ودور مدير النظام.
- [x] إبطال الجلسات عند تغير صلاحيات المستخدم.

## المرحلة 4 — المستخدمون والتدقيق

- [x] عرض المستخدمين والبحث والترقيم.
- [x] إنشاء المستخدم وتعديله وتعطيله دون حذف مادي.
- [x] إدارة أدوار المستخدم ومنع المستخدم من تعديل دوره بنفسه.
- [x] إضافة `AuditLog` للعمليات الإدارية والمالية.
- [x] إلغاء جلسات المستخدم عند تعطيله أو تغيير أدواره.

## المرحلة 5 — السنوات والفترات المالية

- [x] CRUD للسنوات والفترات مع Optimistic Version.
- [x] منع تداخل السنوات والفترات وخروج الفترة عن السنة.
- [x] الإغلاق من الأقدم وإعادة الفتح من الأحدث.
- [x] منع الإغلاق عند وجود مسودات.
- [x] مصالحة المستندات المرحلة قبل إغلاق الفترة.
- [x] Idempotency للإغلاق وإعادة الفتح.
- [x] تسلسلات ذرية لأرقام المستندات دون `MAX()+1`.
- [x] اختبار حجوزات متزامنة دون تكرار.

## المرحلة 6 — دليل الحسابات ومراكز التكلفة

- [x] أنواع الحسابات المرجعية: الأصول والالتزامات وحقوق الملكية والإيرادات والمصروفات.
- [x] شجرة حسابات بحد أقصى 20 مستوى.
- [x] منع الدورات والتداخل بين الشركات.
- [x] منع الترحيل إلى حساب غير نشط أو تجميعي أو غير مسموح بالترحيل إليه.
- [x] تعطيل الحساب المستخدم بدل حذفه.
- [x] شجرة مراكز التكلفة ومنع الدورات وتعطيل المراكز.
- [x] ربط أسطر القيود بالحسابات ومراكز التكلفة بعلاقات شركة مركبة.
- [x] اختبارات العزل والتزامن وتكرار رموز الحسابات.

## المرحلة 7 — القيود اليومية اليدوية

- [x] مسارات العرض والإنشاء والتعديل للمسودات وفق OpenAPI.
- [x] استبدال رأس القيد وبنوده مع حماية `version`.
- [x] التحقق من الفترة والحساب ومركز التكلفة والعملة وسعر الصرف.
- [x] احتساب مبالغ العملة الأساسية بأربع منازل عشرية.
- [x] فرض أن يكون السطر مدينًا أو دائنًا فقط.
- [x] فرض توازن كل قيد بالعملة الأساسية عند الترحيل.
- [x] فصل منشئ القيد عن مرحّله افتراضيًا بإعداد الشركة.
- [x] الترحيل المتزامن الآمن وإعادة نفس استجابة Idempotency.
- [x] إلغاء المسودة وعكس القيد المرحل وإنشاء قيد عكسي.
- [x] علاقات مركبة لمنع العكس بين شركات مختلفة.

## المرحلة 8 — العملاء والخزينة

- [x] CRUD للعملاء مع حساب الذمم وعناوين العميل.
- [x] عنوان رئيسي واحد عند التعديل عبر الخدمة.
- [x] تعطيل العميل دون حذف مادي.
- [x] CRUD للصناديق والحسابات البنكية وربطها بحساب أستاذ صالح.
- [x] التحقق من بيانات الحساب البنكي وإخفاء أرقام الحساب وIBAN.
- [x] Seed لطرق الدفع: نقدي، تحويل بنكي، بطاقة وشيك.
- [x] دعم طرق دفع عامة أو خاصة بالشركة.
- [x] حفظ آخر أربعة أرقام فقط من البيانات الضريبية أو البنكية الحساسة المستخدمة للعرض المقنع.

## المرحلة 9 — سندات القبض

- [x] عرض وإنشاء وتعديل سندات القبض كمسودات.
- [x] حجز رقم سند القبض عند إنشاء المسودة.
- [x] فرض XOR بين العميل والحساب المقابل.
- [x] التحقق من العميل والصندوق/البنك وطريقة الدفع والعملة والفترة والحسابات.
- [x] إلزام المرجع لطرق الدفع التي تتطلبه.
- [x] احتساب مبلغ العملة الأساسية والتحقق من سعر صرف العملة الأساسية.
- [x] توزيعات على أسطر قيود مرحلة تخص العميل والشركة والعملة نفسها.
- [x] إلزام سند العميل بتوزيع كامل يساوي مبلغ السند، مع إبقاء السند غير الموزع للحساب المقابل المباشر فقط.
- [x] منع التوزيع الزائد على رصيد السطر المستهدف.
- [x] توليد قيد متوازن: مدين للصندوق/البنك ودائن لذمم العميل أو الحساب المقابل.
- [x] ترحيل متزامن آمن باستخدام Idempotency.
- [x] إلغاء المسودة وعكس السند المرحل بقيد عكسي.
- [x] عزل الشركة على مستوى الخدمة والمفاتيح الأجنبية المركبة.

## المرحلة 10 — الموردون وسندات الصرف

- [x] إنشاء الموردين وعرضهم وتعديلهم وتعطيلهم وربط كل مورد بحساب دائن صالح للترحيل.
- [x] إنشاء عناوين الموردين وتعديلها وحذفها، مع دعم عنوان الدفع والعنوان الرئيسي.
- [x] حفظ آخر أربعة أرقام فقط من الرقم الضريبي وإرجاعه بصورة مقنّعة.
- [x] عرض وإنشاء وتعديل سندات الصرف كمسودات مع حجز رقم ذري للمستند.
- [x] فرض XOR بين المورد والحساب المقابل والتحقق بعد دمج تعديلات المسودة.
- [x] التحقق من المورد والحساب المقابل والصندوق/البنك وطريقة الدفع والمرجع المطلوب والعملة وسعر الصرف والفترة المالية.
- [x] إلزام سند المورد بتوزيع كامل يساوي مبلغ السند والتحقق من أن العناصر المستهدفة دائنة ومرحلة وتخص المورد والعملة والشركة نفسها، مع إبقاء السند غير الموزع للحساب المقابل المباشر فقط.
- [x] منع التوزيع الزائد على الرصيد القائم مع احتساب سندات الصرف المرحلة الأخرى.
- [x] توليد قيد متوازن: مدين لذمم المورد أو الحساب المقابل، ودائن للصندوق أو البنك.
- [x] ترحيل آمن ومتزامن باستخدام Optimistic Version وIdempotency مع إعادة نفس الاستجابة للمفتاح نفسه.
- [x] إلغاء المسودة وعكس السند المرحل بقيد عكسي داخل فترة مفتوحة.
- [x] صلاحيات مستقلة للموردين وسندات الصرف وسجل تدقيق لجميع العمليات الحساسة.
- [x] عزل بين الشركات في الخدمات والمفاتيح الأجنبية المركبة، مع اختبار محاولة الربط بحساب شركة أخرى.
- [x] تطبيق الترحيل العاشر وSeed الصلاحيات على MariaDB في XAMPP.
- [x] اختبارات تكامل للمورد والعناوين والتحقق والتعديل والإلغاء والترحيل والتزامن والتوزيع الزائد والعكس وتسلسل الأرقام.

## المرحلة 11 — واجهة الموردين وسندات الصرف

- [x] استبدال الصفحة التعريفية بتطبيق تشغيلي عربي متصل فعليًا بالـAPI.
- [x] تسجيل الدخول الآمن واختيار سياق الشركة والاحتفاظ بالسياق داخل الجلسة.
- [x] تنقل جانبي واضح بين الموردين وسندات الصرف مع قائمة هاتف قابلة للطي.
- [x] قائمة الموردين مع البحث والترقيم والتصفية حسب النشاط وحالات التحميل والفراغ والخطأ.
- [x] إنشاء المورد وتعديله وتعطيله مع اختيار حساب الذمم الدائنة وإخفاء الرقم الضريبي.
- [x] عرض تفاصيل المورد وإنشاء عناوينه وتعديلها وحذفها وتعيين العنوان الرئيسي.
- [x] قائمة سندات الصرف مع البحث والترقيم والتصفية بالحالة والتاريخ.
- [x] نموذج سند الصرف للمورد أو الحساب المباشر، والفترة والصندوق/البنك وطريقة الدفع والعملة وسعر الصرف.
- [x] إدارة توزيعات السند مع جمعها والتحقق محليًا من مطابقتها للمبلغ قبل الإرسال.
- [x] عرض تفاصيل السند وإجراءات الترحيل والإلغاء والعكس مع تأكيدات ورسائل عربية لقواعد الأعمال والتعارض والصلاحيات.
- [x] إضافة مرجع العملات النشطة إلى API وعقد OpenAPI لدعم نموذج السند.
- [x] تصميم متجاوب يدعم RTL وإتاحة الاستخدام بعناوين الحقول والأدوار والتنبيهات المناسبة.
- [x] تحقق مرئي فعلي عند عرض 1440×900 وعرض هاتف 390×844 دون تمرير أفقي أو أخطاء متصفح.
- [x] اختبارات وحدة لمنطق الدقة والتوزيعات والحالات ورسائل الأخطاء في الواجهة.

## المرحلة 12 — لوحة التحكم والتقارير المالية

- [x] إضافة صلاحيتين مستقلتين للوحة التحكم وميزان المراجعة وإسنادهما تلقائيًا لدور مدير النظام.
- [x] إضافة مسار لوحة تحكم مقيد بالشركة ونطاق التاريخ يعيد العملة الأساسية والمؤشرات والحركة الشهرية وأحدث السندات.
- [x] احتساب المقبوضات والمدفوعات وصافي التدفق من السندات المرحلة دون جدول ملخصات موازٍ، مع تثبيت الحركة في شهرها الأصلي وإظهار العكس سالبًا في تاريخ العكس.
- [x] عرض عدد الموردين والعملاء النشطين ومسودات سندات القبض والصرف التي تحتاج إلى متابعة.
- [x] إضافة تقرير ميزان مراجعة من أسطر القيود الفعلية، مع تضمين القيود الأصلية والمعكوسة حتى يبقى أثر العكس متوازنًا.
- [x] فرض عزل الشركة والتحقق من ترتيب التاريخ وتقييد نطاق التقرير التفاعلي بحد أقصى 366 يومًا.
- [x] إنشاء واجهة لوحة تحكم عربية متجاوبة ببطاقات مؤشرات ورسم شهري وأحدث النشاطات وروابط للوحدات التشغيلية.
- [x] إنشاء صفحة تقارير بفلاتر تاريخية وجدول للتدفق النقدي وميزان المراجعة وإمكانية تنزيل CSV عربي.
- [x] تحديث عقد OpenAPI ليشمل مسار لوحة التحكم وشكل ميزان المراجعة المنفذ والتحقق من صحة YAML.
- [x] إضافة اختبارات لحساب التجميع الشهري ونطاق التقارير والصلاحيات وتصدير CSV.
- [x] تطبيق الترحيل الحادي عشر على MariaDB والتحقق الوظيفي من تسجيل الدخول واستجابات التقارير الفعلية.

## المرحلة 13 — واجهة العملاء وسندات القبض

- [x] إضافة العملاء إلى التنقل الرئيسي مع قائمة وبحث وترقيم وتصفية حسب حالة النشاط.
- [x] إنشاء العميل وتعديله وتعطيله مع اختيار حساب الذمم المدينة وإخفاء الرقم الضريبي.
- [x] عرض تفاصيل العميل وإدارة عناوينه القانونية وعناوين الفوترة والعناوين الأخرى.
- [x] استكمال حذف عنوان العميل في API مع عزل الشركة وصلاحية الإدارة وسجل التدقيق.
- [x] إضافة سندات القبض إلى التنقل مع البحث والترقيم والتصفية بالحالة والفترة التاريخية.
- [x] إنشاء وتعديل مسودة سند قبض مرتبطة بعميل أو بحساب مقابل مباشر.
- [x] تحميل الفترات المفتوحة والصناديق والبنوك وطرق الدفع والعملات والحسابات والعملاء النشطين داخل نموذج السند.
- [x] دعم المرجع الإلزامي وسعر الصرف والعملة وبيانات الطرف والتوزيعات على أسطر الذمم المدينة.
- [x] عرض تفاصيل سند القبض وحالته ومبلغه ومرجعه وتوزيعاته.
- [x] ربط الترحيل والإلغاء والعكس بحماية الإصدار وIdempotency ورسائل التأكيد والأخطاء العربية.
- [x] توحيد صلاحية مرجع العملات على عرض حسابات الخزينة كي يخدم القبض والصرف دون اعتماد متبادل بين الوحدتين.
- [x] إضافة رسائل تحقق خاصة بالعميل واختبار حذف العنوان ودورة سندات القبض على MariaDB الفعلية.
- [x] دمج الصفحتين مع التصميم العربي المتجاوب والقائمة الجانبية ولوحة التحكم.

## المرحلة 14 — واجهة السنوات والفترات والقيود اليومية

- [x] إضافة السنوات والفترات المالية إلى التنقل الرئيسي مع قائمة مجمعة بحسب السنة وتصفية حالة الفترات.
- [x] إنشاء سنة مالية مع محرر متعدد الفترات وتحقق مسبق من الحدود والتداخل وتكرار الأرقام.
- [x] تعديل السنة والفترة مع Optimistic Version، وإغلاق الفترة وإعادة فتحها مع التأكيد والسبب وIdempotency.
- [x] تضمين الفترات المرتبة داخل استجابة قائمة السنوات لتوحيد عقد الـAPI مع احتياج الواجهة.
- [x] إضافة قائمة القيود اليومية مع البحث والترقيم والتصفية بالحالة والنطاق التاريخي وعرض التفاصيل.
- [x] إنشاء وتعديل مستند قيد متعدد القيود والأسطر مع الحساب ومركز التكلفة والطرف والعملة وسعر الصرف والمدين والدائن.
- [x] حساب الاتزان بالعملة الأساسية في الواجهة، ومنع السطر مزدوج الطرف والمدين/الدائن، والتحقق من اتزان كل قيد على حدة.
- [x] ربط الترحيل والإلغاء والعكس بحماية الإصدار وIdempotency وفصل منشئ القيد عن مرحّله في الخادم.
- [x] معالجة تعارض معاملات إنشاء سندات الصرف المتزامنة بإعادة محاولة محدودة لأخطاء deadlock فقط، وتثبيت اختبار التزامن على MariaDB.
- [x] إضافة رسائل عربية لقواعد الأعمال وتنسيق متجاوب للجداول ومحرر الأسطر.
- [x] إضافة 3 اختبارات واجهة للتقويم المالي والاتزان وسعر الصرف، ونجاح 37 اختبارًا إجمالًا.

## المرحلة 15 — واجهة الدليل المحاسبي والخزينة

- [x] إضافة دليل الحسابات وإدارة الخزينة إلى التنقل الرئيسي مع عناوين وروابط مستقلة.
- [x] عرض دليل الحسابات كشجرة مرتبة مع تحميل جميع الصفحات والبحث والتصفية حسب النشاط.
- [x] إنشاء الحسابات وتعديلها وتعطيلها مع النوع والحساب الأب وطبيعة الترحيل والحساب الرقابي وقواعد منع الدورات والفروع النشطة.
- [x] عرض مراكز التكلفة كشجرة مستقلة مع الإنشاء والتعديل ونقل المركز والتعطيل الموثق بالسبب.
- [x] عرض الصناديق والحسابات البنكية مع البحث والتصفية بالنوع والنشاط وربط حساب أستاذ قابل للترحيل.
- [x] إنشاء وتعديل وتعطيل حسابات الخزينة، وإظهار رقم الحساب وIBAN بصورة مقنّعة مع توضيح أن آخر أربعة أرقام فقط تُحفظ.
- [x] إدارة طرق الدفع الخاصة بالشركة بالإنشاء والتعديل والتعطيل، مع إبقاء الطرق العامة مرجعية للعرض فقط.
- [x] تحديث عقد OpenAPI لمسارات إدارة طرق الدفع وخيار عرض الطرق المعطلة.
- [x] إضافة رسائل عربية لقواعد الشجرة والخزينة والمراجع العامة، واختبارات لترتيب الشجرة والتحقق البنكي ودورة طرق الدفع.
- [x] فحص بصري ووظيفي على الواجهة الفعلية بمقاسي سطح المكتب والهاتف دون إدخال بيانات تجريبية دائمة.

## المرحلة 16 — إدارة المستخدمين والأدوار وإعدادات الشركة

- [x] واجهة مستخدمين متجاوبة تدعم البحث والتصفية بالحالة والترقيم والإنشاء والتعديل والتعطيل وإسناد الأدوار.
- [x] واجهة أدوار وصلاحيات مجمعة حسب الوحدة، مع إنشاء الدور المخصص وتعديل بياناته واستبدال صلاحياته وتعطيله.
- [x] حماية الدور النظامي من التعديل والتعطيل، ومنع المسؤول من تعطيل نفسه أو تغيير أدواره بنفسه.
- [x] إزالة إسنادات الدور عند تعطيله وإبطال جلسات مستخدميه داخل الشركة عند تغيير صلاحياته.
- [x] واجهة للجلسات الشخصية مع عرض النشاط والانتهاء وإلغاء الجلسات غير الحالية.
- [x] تنفيذ `GET/PATCH /companies/current` و`GET/PUT /settings` لإدارة بيانات الشركة وضابط فصل منشئ القيد عن مرحّله.
- [x] إضافة صلاحيات `companies.view` و`companies.update` و`settings.manage` وإسنادها لدور مدير النظام بالترحيل رقم 12.
- [x] تحديث عقد OpenAPI لمسارات الأدوار المخصصة وبيانات الشركة والإعدادات الفعلية.
- [x] معالجة تزامن طلب CSRF الأولي في React Strict Mode لمنع تعارض جلسات ما قبل المصادقة.
- [x] اختبار مباشر على XAMPP وتسجيل الدخول والتنقل بين المستخدمين والأدوار والإعدادات، وفحص العرض الضيق بصريًا.

## المرحلة 17 — الطباعة والأرشفة التاريخية للمستندات

- [x] إضافة نموذج `DocumentPrintArchive` بلقطة JSON ثابتة وبصمة SHA-256 وعدّاد طباعة وتواريخ أول وآخر طباعة وربطه بالشركة والمستند ومنشئ الأرشيف.
- [x] إنشاء اللقطة تلقائيًا داخل معاملة ترحيل سند القبض أو الصرف أو القيد اليومي، وكذلك لمستندات العكس، مع إنشاء كسول للمستندات التاريخية المرحلة قبل هذه المرحلة.
- [x] منع تأثر إعادة الطباعة بتعديلات بيانات الشركة أو الحسابات اللاحقة، والتحقق من سلامة البصمة قبل كل رندر.
- [x] إضافة الصلاحيات `receipts.print` و`payments.print` و`manual_journals.print` وإسنادها إلى مدير النظام، وتسجيل `DOCUMENT_PRINT_ARCHIVED` و`DOCUMENT_PDF_PRINTED` في سجل التدقيق.
- [x] تنفيذ مسارات `GET /receipts/:id/pdf` و`GET /payments/:id/pdf` و`GET /manual-journals/:id/pdf` مع عزل الشركة وترويسات معرف الأرشيف والبصمة.
- [x] بناء قالب PDF عربي A4 يتضمن بيانات الشركة ورقم المستند وحالته والعملية والقيود والتوقيعات وبصمة الأرشيف، باستخدام خط عربي مضمن دون اعتماد على خطوط نظام التشغيل.
- [x] ربط زر طباعة PDF بتفاصيل المستندات المرحلة والمعكوسة في الواجهات الثلاث، مع تنزيل الاسم القادم من الخادم ومعالجة الأخطاء العربية.
- [x] تحديث عقد OpenAPI للمسارات والصلاحيات والاستجابة الثنائية وترويسات الأرشيف، والتحقق من صحة YAML.
- [x] إضافة اختبار رندر مستقل واختبار تكامل يثبت ثبات اللقطة بعد تغيير مصدر البيانات وتطابق البصمة وزيادة عداد الطباعة.
- [x] فحص القالب بصريًا بعد تحويله إلى PNG، والتحقق من أنه صفحة واحدة واضحة بلا قص أو محارف مفقودة، مع نجاح تدقيق الاعتماديات دون ثغرات معروفة.

## المرحلة 18 — سجل التدقيق التشغيلي

- [x] تنفيذ `AuditService` بعزل إلزامي حسب الشركة وترتيب ثابت من الأحدث، مع البحث والترقيم والتصفية بالمستخدم والإجراء ونوع الكيان والنطاق الزمني.
- [x] إضافة مسارات القائمة والتفاصيل وخيارات المرشحات وتصدير CSV، مع صلاحيتين مستقلتين `audit_logs.view` و`audit_logs.export` وإسنادهما إلى مدير النظام بالترحيل رقم 14.
- [x] حماية تصدير CSV من حقن صيغ جداول البيانات، وإضافة BOM لدعم العربية، وحد أقصى 10,000 صف وترويسات تبين العدد والاقتطاع.
- [x] تسجيل كل عملية تصدير كحدث `AUDIT_LOG_EXPORTED` مع المرشحات والعدد، ومنع الوصول إلى أحداث الشركات الأخرى حتى عند استخدام معرّف مباشر.
- [x] تصحيح عقد OpenAPI القديم ليتطابق مع النموذج الفعلي، وإضافة عقود الخيارات والتفاصيل والتصدير والصلاحيات والتحقق من صحة YAML.
- [x] إضافة واجهة عربية لسجل التدقيق إلى التنقل الرئيسي، مع مرشحات متجاوبة وجدول مرقم وتسميات عربية وعرض JSON للتفاصيل وروابط انتقال إلى الوحدات المرتبطة.
- [x] معالجة زيادة عناصر القائمة الجانبية بإضافة تمرير داخلي مستقل يحافظ على بطاقة الشركة، وإضافة تخطيطات مرشحات أحادية وثنائية الأعمدة للشاشات الضيقة.
- [x] إضافة 3 اختبارات تكامل فعلية على MariaDB تثبت العزل والتصفية والترقيم والبحث والتفاصيل والخيارات والصلاحيات وصحة CSV وتدقيق التصدير.
- [x] تحقق متصفح فعلي من تسجيل الدخول وعرض القائمة والتفاصيل وتطبيق مرشح الإجراء وتنزيل CSV على خادم XAMPP المحلي.

## المرحلة 19 — القوائم المالية والمقارنات

- [x] تنفيذ تقرير المركز المالي في تاريخ محدد من القيود المرحّلة والمعكوسة، مع تجميع شجرة الحسابات وإظهار الأرباح المتراكمة غير المقفلة وفحص المعادلة المحاسبية آليًا.
- [x] تنفيذ قائمة الدخل ضمن نطاق لا يتجاوز 366 يومًا، مع الإيرادات والمصروفات وصافي الربح أو الخسارة ومقارنة اختيارية بفترة سابقة.
- [x] تنفيذ تقرير الأستاذ للحساب أو العميل أو المورد، شاملًا الرصيد الافتتاحي والحركات والرصيد الجاري والختامي والترقيم.
- [x] إضافة صلاحيات مستقلة لعرض المركز المالي وقائمة الدخل والأستاذ وتصدير القوائم، وإسنادها إلى مدير النظام بالترحيل رقم 15.
- [x] إضافة تصدير CSV وExcel XLSX حقيقي وPDF عربي، وتسجيل `FINANCIAL_REPORT_EXPORTED` في سجل التدقيق.
- [x] تحديث واجهة التقارير إلى تبويبات للتدفق النقدي وميزان المراجعة والمركز المالي وقائمة الدخل، مع المقارنة وإظهار الأرصدة الصفرية والانتقال إلى كشف الحساب.
- [x] تحديث عقد OpenAPI بأنواع الاستجابات ومسارات العرض والتصدير والصلاحيات.
- [x] إضافة اختبارات للحسابات الهرمية والطبيعة المحاسبية والمقارنة وملفات CSV وXLSX وPDF وصلاحيات المسارات والتحقق من الاستعلامات.
- [x] تحقق وظيفي مباشر من بيانات التجربة: الأصول `315750.0000`، حقوق الملكية `315750.0000`، صافي الربح `15750.0000`، وفرق المعادلة `0.0000`.
- [x] تحقق متصفح فعلي من المركز المالي وقائمة الدخل والمقارنة وكشف الأستاذ، وفحص PDF بصريًا بعد تحويله إلى PNG.

## المرحلة 20 — تقرير دفتر اليومية التشغيلي

- [x] تنفيذ `GET /reports/journal` من القيود الفعلية مع عزل الشركة، ونطاق تاريخ لا يتجاوز 366 يومًا، وترتيب ثابت من الأحدث.
- [x] دعم التصفية بنوع المستند والحالة والحساب والبحث في رقم المستند أو وصفه أو بيان القيد، مع ترقيم الصفحات وإجمالي عدد النتائج.
- [x] احتساب إجمالي المدين والدائن لكل قيد ولجميع النتائج المصفاة، وإرجاع مؤشر اتزان صريح لكل صف.
- [x] تنفيذ `GET /reports/journal/csv` بحد أقصى 10,000 قيد، وترويسات عربية وBOM وحماية من حقن صيغ الجداول وترويسات العدد والاقتطاع.
- [x] إضافة صلاحيتين مستقلتين `reports.journal.view` و`reports.journal.export` وإسنادهما إلى مدير النظام بالترحيل رقم 16.
- [x] تسجيل تصدير التقرير كحدث `FINANCIAL_REPORT_EXPORTED` مع الصيغة والمرشحات بعد تحويل معرفات `BigInt` إلى قيم JSON آمنة.
- [x] إضافة تبويب دفتر اليومية إلى واجهة التقارير مع مرشحات المستند والحالة والحساب والبحث، وجدول متجاوب، وإجماليات، وترقيم، وتنزيل CSV.
- [x] تحديث عقد OpenAPI بشكل التقرير ومسار CSV والصلاحيات والمرشحات، وإضافة اختبارات للمسارات والتصدير وحماية CSV.
- [x] تطبيق الترحيل على MariaDB وإعادة تجهيز بيانات العرض، والتحقق الحي من 14 قيدًا متوازنًا بإجمالي `590500.0000` لكل طرف.
- [x] فحص الواجهة فعليًا على العرض المكتبي والهاتف، ومعالجة الالتفاف لمنع التمرير الأفقي الناتج من تعدد المرشحات.

## المرحلة 21 — فواتير المبيعات والذمم المدينة

- [x] إضافة نماذج `TaxRate` و`SalesInvoice` و`SalesInvoiceLine` وعلاقات العميل والعملة وحساب الإيراد ومركز التكلفة وحساب ضريبة المخرجات وسطر الذمم والإشعار الدائن المرجعي.
- [x] تنفيذ إنشاء وتعديل مسودات الفواتير والإشعارات الدائنة، وحساب الكمية وسعر الوحدة والخصم والصافي وضريبة القيمة المضافة والإجمالي والعملة الأساسية بدقة `DECIMAL`.
- [x] تنفيذ الترحيل المحاسبي المتوازن للفواتير والإشعارات الدائنة، والإلغاء والعكس الآمن وOptimistic Version وIdempotency، ومنع عكس الفاتورة عند وجود تحصيلات مرحلة مرتبطة.
- [x] ربط سند القبض بالفاتورة المفتوحة من الواجهة بدل إدخال رقم سطر القيد يدوياً، مع اقتراح مبلغ التوزيع ومنع تكرار اختيار الفاتورة.
- [x] تنفيذ تقرير أرصدة العملاء وأعمار الديون حتى تاريخ محدد بالعملة الأساسية، بعد خصم التحصيلات والإشعارات الدائنة المرحلة.
- [x] إضافة ثماني صلاحيات مستقلة للفواتير والضريبة وتقارير الذمم، وإسنادها إلى مدير النظام ودور المحاسب التجريبي، مع أحداث تدقيق للإنشاء والتعديل والترحيل والإلغاء والعكس وإدارة الضرائب.
- [x] إضافة واجهة عربية متجاوبة للفواتير والبنود والضريبة والإشعارات الدائنة وأعمار الديون، مع بيانات تجربة تشمل فواتير مفتوحة وجزئية ومسودة وإشعاراً دائناً وتحصيلاً مرتبطاً.
- [x] تحديث عقد OpenAPI بالمسارات والطلبات والاستجابات وأنواع المستندات والصلاحيات، وإضافة اختبارات حسابية وتكاملية فعلية لدورة المبيعات.
- [x] نجاح 66 اختبارًا في 22 ملفًا عند تشغيل اختبارات MariaDB تسلسليًا، ونجاح TypeScript والبناء وترحيل قاعدة البيانات والتحقق الحي من الواجهة.

## المرحلة 22 — فواتير المشتريات والذمم الدائنة

- [x] إضافة نماذج `PurchaseInvoice` و`PurchaseInvoiceLine` وعلاقات المورد والعملة وحساب المصروف أو الأصل ومركز التكلفة وحساب ضريبة المدخلات وسطر الذمم والإشعار المدين المرجعي.
- [x] تنفيذ إنشاء وتعديل مسودات فواتير الموردين والإشعارات المدينة، مع رقم فاتورة المورد والخصومات والضريبة والإجماليات والعملة الأساسية بدقة `DECIMAL`.
- [x] تنفيذ الترحيل المتوازن: مدين للمصروفات أو الأصول وضريبة المدخلات ودائن لذمم المورد، مع عكس الاتجاه للإشعار المدين.
- [x] تنفيذ الإلغاء والعكس وOptimistic Version وIdempotency، ومنع تجاوز الإشعارات لقيمة الفاتورة أو عكس فاتورة مرتبطة بسداد مرحّل.
- [x] ربط سند الصرف بفواتير المورد المفتوحة من الواجهة، مع عرض رقم الفاتورة ومرجع المورد والمتبقي واقتراح مبلغ التوزيع.
- [x] تنفيذ تقرير أرصدة الموردين وأعمار الديون حتى تاريخ محدد بالعملة الأساسية بعد خصم المدفوعات والإشعارات المدينة المرحلة.
- [x] إضافة ثماني صلاحيات مستقلة للفواتير وضريبة المدخلات وتقارير الذمم، وإسنادها إلى مدير النظام ودور المحاسب التجريبي، مع سجل تدقيق للعمليات الحساسة.
- [x] إضافة واجهة عربية متجاوبة للفواتير والبنود وضريبة المدخلات والإشعارات المدينة وأعمار الديون، وإصلاح تجاوز العرض الأفقي للقائمة الجانبية المخفية على الهاتف.
- [x] تجهيز بيانات تجربة تشمل ثلاث فواتير مرحلة وفاتورة مسودة وإشعارًا مدينًا وسدادًا جزئيًا مرتبطًا وحساب ضريبة مدخلات.
- [x] تحديث عقد OpenAPI بالمسارات والطلبات والاستجابات وأنواع المستندات والصلاحيات.
- [x] إضافة اختبارات حسابية وتكاملية فعلية لدورة المشتريات، ونجاح 66 اختبارًا في 22 ملفًا مع نجاح TypeScript والبناء والترحيلات والتحقق الحي من الواجهة.

## المرحلة 23 — طباعة فواتير المشتريات

- [x] توسيع لقطة الطباعة التاريخية لتشمل المورد ورقم فاتورته والاستحقاق والعملة والبنود والخصومات وضريبة المدخلات والإجماليات والقيود المحاسبية.
- [x] إنشاء الأرشيف تلقائيًا داخل معاملة ترحيل فاتورة المشتريات أو الإشعار المدين، مع بصمة SHA-256 ثابتة وعداد إعادة الطباعة وسجل تدقيق.
- [x] إضافة صلاحية `purchase_invoices.print` ومسار `GET /purchase-invoices/:id/pdf` وزر تنزيل PDF في تفاصيل المستندات المرحلة والمعكوسة.
- [x] تحديث عقد OpenAPI وإضافة اختبار رندر واختبار تكامل للبصمة والعداد ومحتوى لقطة فاتورة المورد.

## المرحلة 24 — سجل الأمان والتنبيهات

- [x] إضافة نموذج `SecurityEvent` المعزول بالشركة مع درجات `INFO / WARNING / HIGH / CRITICAL` وبيانات المستخدم والجلسة والجهاز من دون رموز المصادقة.
- [x] تسجيل نجاح وفشل الدخول، وقفل الحساب، ومحاولة الدخول لحساب مقفل أو معطل، واختيار الشركة، والخروج، وإلغاء الجلسة.
- [x] إضافة صلاحيتي `security_events.view` و`security_events.acknowledge` ومسارات القائمة والملخص والخيارات والإقرار، مع سجل تدقيق للإقرار.
- [x] إضافة واجهة عربية تشغيلية تعرض عدادات 24 ساعة، التنبيهات غير المقر بها، مرشحات الخطورة والحدث والمستخدم والتاريخ، وتفاصيل آمنة.
- [x] تحديث OpenAPI والبذور، وإضافة اختباري تكامل فعليين يثبتان القفل والتنبيه والإقرار والصلاحيات.

## المرحلة 25 — الأساس الإنتاجي متعدد الشركات

- [x] إضافة رموز ثابتة وفريدة للمؤسسات والشركات مع ترحيل Backfill آمن للبيانات القائمة.
- [x] تنفيذ خدمة تجهيز شركة ذرية ومتكررة التشغيل تنشئ المؤسسة والشركة وإسناد المدير ودور الشركة وصلاحياته والمرجعيات العامة.
- [x] دعم هوية مستخدم عالمية واحدة مرتبطة بعدة شركات من دون تغيير كلمة مرور المستخدم الموجود أو مضاعفة الإسنادات والأدوار.
- [x] إضافة أمر إداري محمي بتأكيد صريح ومتغيرات مؤقتة لتجهيز شركة جديدة دون تشغيل Demo Seed.
- [x] فرض بوابات إعداد الإنتاج: HTTPS وSecure Cookie وreverse proxy وقاعدة البيانات.
- [x] إضافة `/live` و`/ready` وفحص قاعدة البيانات بمهلة، وRate Limiting عام ومشدد للمصادقة، وRequest ID وسجلات JSON.
- [x] إضافة إغلاق سليم لخادم HTTP وPrisma عند `SIGTERM` و`SIGINT`.
- [x] تثبيت إصدارات الاعتماديات بدل `latest` وإضافة CI على MySQL 8.4 ونماذج PM2 وNginx وبيئة الإنتاج.
- [x] تصحيح خطأ OpenAPI وتوافق مخططات PATCH لسندات القبض والصرف مع التحديث الجزئي.
- [x] إضافة اختبارات إعدادات الإنتاج والجاهزية وRate Limit والإغلاق وتجهيز شركتين لهوية واحدة والتكرار الآمن.

## المرحلة 26 — إغلاق بوابات البنية التحتية

- [x] تشغيل MySQL Community Server 8.4.11 LTS محمول ومعزول والتحقق من بصمة الحزمة الرسمية وإصدار الخادم الفعلي.
- [x] اكتشاف تعارض MySQL 8 بين أعمدة CHECK وإجراءات المفاتيح الأجنبية وإصلاح قيود الجلسات والفترات والمستندات وطرق الدفع والقبض والصرف مع منع تغيير أو حذف المعرفات التاريخية.
- [x] تطبيق 22 ترحيلًا من قاعدة فارغة وتشغيل Seed وDemo Seed و66 اختبار API تسلسليًا على MySQL 8.4.11.
- [x] جعل بصمة لقطات الطباعة Canonical ومستقلة عن ترتيب مفاتيح JSON الذي يعيد MySQL تنظيمه، مع دعم بصمات MariaDB القديمة.
- [x] إنشاء نسخ منطقي متسق ومضغوط ومشفر بإطارات AES-256-GCM وManifest يحوي SHA-256 والترحيلات وأعداد الصفوف وإجمالي المدين والدائن.
- [x] تنفيذ استعادة لا تقبل إلا قاعدة فارغة وتتطلب تأكيدًا صريحًا، وتجربتها فعليًا مع 39 جدولًا و22 ترحيلًا ومقارنة بيانات التحقق بعد الاستعادة.
- [x] تشديد CI بتثبيت MySQL 8.4.11 والتحقق من الإصدار والـseed الكامل والاختبارات التسلسلية والتدقيق الأمني وفحص إنتاجي بعد إزالة أدوات البناء وإنتاج Artifact لينكس.
- [x] حصر تنبيه `deepmerge-ts` داخل Prisma CLI التطويري، ومنع High في حزمة التشغيل وCritical في البناء، وتوثيق الاستثناء المؤقت والمراجعة.
- [x] تقييد سكربتات تثبيت npm بأربع حزم مثبتة بالإصدار بدل السماح العام.
- [x] تثبيت CI على npm 12.0.2 وفشلها عند ظهور أي سكربت تثبيت غير معتمد.
- [x] تقسيم واجهة React حسب الصفحات؛ انخفضت الحزمة الرئيسية من 846KB إلى 225KB وأصبحت وحدات الصفحات بين 3KB و29KB.
- [x] تضمين خطي الواجهة العربيين داخل البناء وإزالة الاعتماد على Google Fonts، مع CSP متوافق مع الرسوم الديناميكية.
- [x] تضمين أدوات النسخ والاستعادة ووحدات systemd ودليل التشغيل داخل Artifact الإنتاج نفسه.
- [x] إغلاق تحذيرات Redocly الـ42؛ أصبح عقد OpenAPI صالحًا دون أخطاء أو تحذيرات.

## المرحلة 27 — سلامة الإصدار والنشر الذري

- [x] إنشاء Release Manifest مثبتًا للإصدار والـcommit والمنصة وإصدار Node وحجم وبصمة SHA-256 لكل ملف.
- [x] رفض الملفات الزائدة أو الناقصة أو المعدلة، والروابط الرمزية، ومسارات الاجتياز، والحزم المبنية لنظام تشغيل أو معمارية مختلفة.
- [x] جعل تجهيز مجلد الإصدار ذريًا مع تنظيف المجلد الجزئي عند الفشل ومنع استبدال مخرج موجود.
- [x] تثبيت Node 24.19.0 في CI وإنشاء أرشيف Linux x64 قابل لإعادة الإنتاج بترتيب وملكية وتوقيت ثابتة و`gzip -n`.
- [x] إعادة فك Artifact داخل CI وإعادة فحص Manifest وSHA-256 قبل رفعه.
- [x] تنفيذ نشر Linux محمي ببصمة موثوقة وتأكيد صريح وقفل تزامن وتبديل ذري لرابط `current` وفحص `/ready` ورجوع آلي، مع بوابتي `bash -n` وShellCheck 0.11.0 المثبتة بالبصمة داخل CI.
- [x] تنفيذ رجوع يدوي محمي بتأكيد صريح، مع استعادة الإصدار النشط تلقائيًا إذا فشل الإصدار السابق في الجاهزية.
- [x] تجربة حقيقية على شجرة اعتماديات إنتاج منقاة: 3,867 ملفًا بنحو 130.2MB، ثم إنشاء أرشيف بنحو 44MB وفكه والتحقق منه وتشغيل Argon2 من داخله.

## المرحلة 28 — مطابقة iFastNet والهوية المحايدة

- [x] إزالة اسم العمل السابق من النصوص والمسارات والنطاقات البرمجية واعتماد اسم وظيفي محايد قابل للضبط من متغيرات البيئة.
- [x] إعادة توليد عينات PDF بالهوية المحايدة والتحقق بصريًا من الصفحات الثلاث.
- [x] فحص خدمة iFastNet الفعلية دون تعديل الموارد القائمة: cPanel 138 وNode.js Selector وSSH وTerminal وGit متاحة.
- [x] تثبيت هدف التشغيل على Node.js 22.23.2 وكشف أن محرك البيانات الفعلي MariaDB 10.11.11.
- [x] إضافة بوابة CI مستقلة تطبق جميع الترحيلات والـSeed والاختبارات والبناء على Node.js 22.23.2 وMariaDB 10.11.11.
- [x] جعل API يخدم واجهة React المبنية ويدعم SPA fallback كي يعمل كتطبيق cPanel واحد دون Nginx أو PM2.
- [x] إضافة Seed مرجعيات إنتاجي لا ينشئ شركة أو مستخدمًا تجريبيًا، وتشغيل تجهيز الشركات من JavaScript المترجم داخل Artifact.
- [x] إضافة تثبيت ورجوع ذريين خاصين بـPassenger، مع فحص Manifest وSHA-256 و`/ready` ورجوع آلي.
- [x] توثيق مسار iFastNet وحماية تطبيق `tarafu` وقواعد البيانات القائمة من أي تعديل.
- [x] نجاح البوابتين على GitHub والتحقق من Artifact Linux ذي Manifest وبصمة SHA-256 قبل إنشاء موارد الإنتاج.
- [x] إنشاء نطاق وتطبيق وقاعدة إنتاج مستقلة: `accounting.doralashab.com` و`/home/doralash/accounting-app` وقاعدة `doralash_mcapprod`؛ أثبت تشغيل CI رقم 74 تطبيق 33 Migration والنشر والجاهزية من دون لمس تطبيق `tarafu` أو قواعده.

## المرحلة 29 — تعدد العملات الإنتاجي لكل شركة

- [x] إضافة كتالوج مرجعي لعشر عملات وتحديث Seed المرجعيات دون إنشاء بيانات تشغيلية تجريبية.
- [x] إضافة تفعيل مستقل للعملات لكل شركة، مع إبقاء العملة الأساسية مفعلة تلقائيًا للشركات القائمة والجديدة.
- [x] إضافة أسعار صرف مؤرخة ومقيدة بالشركة والعملة، ومصدر اختياري، ومنع تسجيل سعر مستقل للعملة الأساسية.
- [x] إضافة حل السعر النافذ بأحدث تاريخ لا يتجاوز تاريخ المستند، مع عزل كامل بين الشركات.
- [x] إضافة صلاحيتي عرض وإدارة العملات ومنحهما لأدوار المدير النظامية وتسجيل التفعيل والأسعار في سجل التدقيق.
- [x] تقييد القيود والفواتير وسندات القبض والصرف بالعملات المفعلة للشركة بدل الكتالوج العالمي.
- [x] إضافة واجهة إعداد العملات والأسعار، واقتراح السعر المؤرخ تلقائيًا في المستندات مع السماح بتعديل لقطة السعر قبل الترحيل.
- [x] تحديث عقد OpenAPI ودليل التشغيل وإضافة اختبارات تكامل للعملة الأساسية والتأريخ والتدقيق والعزل.

## المرحلة 30 — دليل الحسابات الافتراضي القابل للتخصيص

- [x] تعريف قالب ثابت الإصدار من 60 حسابًا عامًا يناسب غالبية المنشآت الصغيرة ويغطي القوائم المالية والذمم والضريبة والمخزون والأصول الثابتة وتكلفة النشاط والمصروفات.
- [x] تطبيق القالب تلقائيًا وذريًا عند تجهيز شركة جديدة، دون إعادة فرضه عند إعادة تشغيل التجهيز لشركة قائمة.
- [x] إضافة فحص وتطبيق اختياري للشركات القائمة يضيف الحسابات المفقودة ويربط الرموز الموجودة من دون استبدال تخصيصات العميل.
- [x] حفظ هوية القالب مستقلة عن الرمز والاسم والبنية لمنع التكرار بعد التخصيص، مع إبقاء الحساب المعطل معطلًا وإمكان استعادة الحساب المحذوف.
- [x] السماح بتعديل رمز الحساب، وإضافة حذف آمن يمنع الحساب المستخدم أو ذي الفروع ويسجل السبب والهوية السابقة في سجل التدقيق.
- [x] إضافة صلاحيتي `accounts.template.apply` و`accounts.delete` ومسارات API وواجهة عربية توضح حالة القالب وتدعم الإكمال والتعديل والحذف.
- [x] إضافة اختبارات عقد القالب وتكامل MariaDB للتكرار الآمن والتخصيص والحذف والاستعادة وتجهيز شركتين، وتحديث OpenAPI ودليل التشغيل.

## المرحلة 31 — حراسة حدود API وتطابق OpenAPI

- [x] إضافة معالجة مركزية لأخطاء Zod غير الملتقطة، بما يشمل مدخلات المصادقة العامة، وإعادتها كـ`400 VALIDATION_ERROR` مع `requestId` وأخطاء حقول آمنة.
- [x] تصنيف JSON غير الصالح كـ400، وجسم الطلب الأكبر من 1MB كـ413، والترميز غير المدعوم كـ415 دون كشف رسالة المحلل أو تحويل أخطاء العميل إلى 500.
- [x] تقديم وسيط معرّف الطلب على محلل JSON حتى تحمل أخطاء التحليل والحجم معرّف تتبع وتسجل كطلبات مكتملة عند تفعيل السجل.
- [x] تصحيح عقد اختيار الشركة إلى `PUT /auth/context` واستجابة 204، وإزالة `/auth/me` و`/dashboard/summary` والمخططات القديمة غير المنفذة لصالح المسارات التشغيلية الفعلية.
- [x] إضافة اختبار تطابق آلي يقارن 143 عملية HTTP في جميع Routers مع عقد OpenAPI ويمنع انحراف المسار أو الطريقة، مع تطبيع أسماء معاملات المسار.
- [x] إضافة 4 اختبارات للمدخلات العامة وJSON والحجم وتطابق العقد، والتحقق من TypeScript والبناء وRedocly دون إضافة ترحيل قاعدة بيانات.

## المرحلة 32 — توليد أنواع وGuards من OpenAPI

- [x] إضافة مولد حتمي يقرأ OpenAPI 3.1 بواسطة `yaml@2.9.0` المثبت كاعتمادية تطوير، ويحوّل مخططات الطلب المحددة إلى Zod Guards وأنواع TypeScript مستنتجة منها.
- [x] بدء التغطية بالعمليتين `login` و`selectCompanyContext` وربطهما آليًا بمكوّني `LoginRequest` و`SelectCompanyRequest` بدل تكرار المخططات يدويًا في Router.
- [x] الحفاظ على توافق تسجيل الدخول بتثبيت حدود التنفيذ السابقة في العقد: بريد حتى 320 حرفًا وكلمة مرور من 1 إلى 1024 حرفًا، ثم توليد الحارس من هذه القيود.
- [x] تضمين بصمة SHA-256 للعقد في الملف المولد، وإضافة `contracts:generate` لإعادة التوليد و`contracts:check` لرفض الملف المفقود أو المتقادم.
- [x] إضافة بوابة فحص المخرجات المولدة إلى مهمتي MariaDB وMySQL في GitHub Actions قبل TypeScript والاختبارات.
- [x] إضافة 6 اختبارات للمخططات المولدة وربطها الفعلي بمسارات HTTP وتقادم الملف وتأثر المخرجات بتغير قيود العقد.
- [x] نجاح اختبارات TypeScript والبناء وRedocly وفحص التوليد دون ترحيل قاعدة بيانات جديد.

## المرحلة 33 — توسيع التوليد إلى أجسام طلبات الشركة

- [x] توسيع قائمة العمليات المولدة من عمليتين إلى ست لتشمل `updateCurrentCompany` و`replaceCompanySettings` و`replaceCompanyCurrencies` و`upsertCompanyExchangeRate`، مع أنواع TypeScript مستنتجة وحراس Zod حتميين من مكونات OpenAPI.
- [x] دعم `x-trim` في مولد النصوص وقيود `minItems` و`maxItems` و`uniqueItems` للمصفوفات، ثم تمثيل تنظيف اسم الشركة والمنطقة الزمنية ومصدر سعر الصرف في العقد بدل إبقائه سلوكًا يدويًا مخفيًا.
- [x] توحيد العقد مع قواعد التنفيذ: إعداد واحد مدعوم بالضبط، وسعر صرف موجب بثماني منازل، مع إبقاء تكرار معرّفات العملات مقبولًا للتوافق لأن الخدمة تزيل التكرار عند الحفظ.
- [x] استبدال مخططات Zod اليدوية الأربعة في `company-router` بالمخرجات المولدة، والإبقاء على معرّفات النقل نصية وفق OpenAPI ثم تحويلها صراحةً إلى `bigint` عند حد الخدمة.
- [x] إضافة 8 اختبارات سلوكية جديدة للمخططات المولدة ومسارات HTTP تثبت التنظيف والحدود ورفض الصفر والتحويل إلى `bigint` والتوافق مع تكرار العملات، مع استمرار بوابة تقادم المخرجات في مهمتي MariaDB وMySQL.
- [x] نجاح 115 اختبارًا في 37 ملفًا وفحص TypeScript والبناء وRedocly وفحص التوليد دون ترحيل قاعدة بيانات جديد.

## المرحلة 34 — أساس تعدد اللغات ودعم الريال اليمني

- [x] إضافة الريال اليمني `YER` باسم «ريال يمني» ودقة منزلتين إلى Seed المرجعيات المتكرر التشغيل، مع اختبار يثبت وجوده وتفرد أكواد العملات وصيغتها.
- [x] إنشاء قاموسي ترجمة مستقلين `ar` و`en` بمفاتيح typed متطابقة، ومترجم يدعم القيم المتغيرة واختبارات لاكتمال القواميس والاستيفاء واللغة الافتراضية.
- [x] إضافة `I18nProvider` يحفظ اختيار اللغة ويضبط `lang` و`dir` وتنسيق الأرقام والتواريخ، ومبدّل لغة في الدخول واختيار الشركة والشريط العلوي.
- [x] نقل نصوص غلاف التطبيق وتسجيل الدخول واختيار الشركة وإعدادات الشركة والمكونات العامة من JSX إلى القواميس؛ الصفحات التشغيلية المتبقية مسجلة كمرحلة لاحقة وليست مدعاة بأنها مترجمة.
- [x] تقييد مساحة العمل إلى 1240px وضبط اتجاه الشريط الجانبي في LTR ومنع تمدد نموذج الدخول على الهاتف، مع تحقق بصري عربي وإنجليزي عند 390×844 و1280×720 دون تمرير أفقي.
- [x] توثيق خطة مراحل العملات المخصصة والتسجيل الذاتي واستكمال الترجمة ونظام الواجهات وE2E في `docs/product-roadmap.md`.
- [x] نجاح 120 اختبارًا في 39 ملفًا وفحص TypeScript والبناء وفحص التوليد دون Migration جديد؛ يحتاج نشر `YER` إلى تشغيل Seed المرجعيات بعد الإصدار.

## المرحلة 35 — العملات المخصصة المملوكة للشركة

- [x] توسيع `Currency` بنطاق `GLOBAL / COMPANY` ومفتاح نطاق ومالك شركة، واستبدال تفرد الرمز العالمي بتفرد مركب يسمح للشركات المختلفة بالرمز الخاص نفسه من دون تصادم.
- [x] إضافة الترحيل `20260821200000_company_owned_currencies` المتوافق مع MariaDB وMySQL، مع مفتاح خارجي للمالك وقيود فهرسة واتساق لا تغيّر العملات العالمية القائمة.
- [x] تحديث Seed المرجعيات والتجهيز وبيانات التطوير لاستخدام مفتاح العملة العالمية المركب، وإضافة صلاحية `currencies.create` وتوريثها من `currencies.manage` للأدوار القائمة.
- [x] إضافة `POST /company-currencies` إلى OpenAPI 1.4.4 وحارس الطلب المولد؛ ينشئ العملة وتفعيلها وسجل التدقيق ذريًا، ويرد `409 CURRENCY_CODE_EXISTS` عند التكرار أو السباق.
- [x] إغلاق مسارات التسريب بعزل الكتالوج والتفعيل وحفظ/حل أسعار الصرف وقائمة العملات التشغيلية وفق مالك الشركة، ورفض تمرير معرّف عملة خاصة من شركة أخرى.
- [x] إبقاء العملة الأساسية مفعلة، ومنع تعطيل عملة مستخدمة في القيود أو سندات القبض والصرف أو فواتير المبيعات والمشتريات، مع اختبار وحدة يمنع حدوث التحديث أو التدقيق عند الرفض.
- [x] إضافة نموذج إنشاء عملة داخل إعدادات الشركة، ووسم العملات المخصصة، وقواميس عربية وإنجليزية كاملة للنصوص الجديدة، وتخطيط متجاوب من أربعة أعمدة إلى عمود واحد.
- [x] فحص المكوّن الفعلي مقابل API محلي معزول: نجح فتح النموذج وإنشاء عملة وإشعار النجاح وتحديث الكتالوج، ولم يظهر تمرير أفقي عند 390×844 أو 1280×720 بالعربية والإنجليزية.
- [x] حزمة من 126 حالة اختبار في 40 ملفًا: نجحت 89 محليًا وتجاوزت Vitest الحالات الـ37 المعتمدة على قاعدة البيانات لعدم ضبط `RUN_DB_TESTS`، مع نجاح فحص TypeScript وPrisma والعقد المولد وبناء API وReact/Vite.

## المرحلة 36 — التسجيل الذاتي الآمن وتجهيز الشركة الأولى

- [x] إضافة `RegistrationRequest` بحالات الطلب والتسليم والتواريخ وروابط الكيانات المجهزة، و`RegistrationEvent` عام يسجل المحاولات قبل وجود شركة ببصمة HMAC ذات مفتاح إنتاج مستقل للبريد بدل تخزينه في الحدث، عبر الترحيل `20260821220000_self_registration`.
- [x] عدم إنشاء مستخدم أو مؤسسة أو شركة قبل إثبات ملكية البريد؛ كلمة المرور تخزن مؤقتًا بصيغة Argon2 وتمسح من طلب التسجيل بعد النجاح، ورمز البريد عشوائي لا يخزن إلا كبصمة SHA-256 ويوضع في URL Fragment حتى لا يظهر في طلبات HTTP.
- [x] إعادة استخدام `CompanyProvisioningService` بعد فصل مدخلاته المعدة، مع خيار يمنع إسناد هوية موجودة في السياق العام، وتنفيذ إنشاء الشركة والمدير والدور وجميع الصلاحيات ودليل الحسابات وإتمام الطلب داخل معاملة ذرية واحدة.
- [x] إضافة Claim ذري للتجهيز يمنع المضاعفة تحت التزامن، وانتظار المحاولة الجارية أو إرجاع `IN_PROGRESS`، وإعادة نتيجة الشركة نفسها عند إعادة فتح رابط مكتمل، واستئناف Claim عالق بعد خمس دقائق.
- [x] إضافة تسليم بريد خارج المعاملة بحالات `PENDING/SENT/FAILED` وإعادة إرسال تدور الرمز، وتنظيف للطلبات غير المكتملة الأقدم من مدة الاحتفاظ، مع محول Resend للإنتاج ومحول سجل للتطوير فقط.
- [x] حماية `POST /auth/register` و`/resend` و`/verify` بجلسة `PRE_AUTH` وCSRF وحد IP مستقل، واستجابة بدء/إعادة إرسال موحدة حتى للبريد الموجود، مع تسجيل محاولات التعداد والأخطاء وإضافة `SELF_REGISTRATION_COMPLETED` إلى سجل أمان الشركة.
- [x] إضافة خيارات عامة للعملات العالمية الفعالة والمناطق الزمنية واللغتين والقالب، وعقد OpenAPI 1.5.0 وثلاثة Request Guards مولدة جديدة، ليصبح التطابق الآلي 148 عملية HTTP وقائمة الحراس 10 عمليات.
- [x] إضافة واجهة تسجيل مستقلة بقاموسي العربية والإنجليزية، واختيار اللغة والمنطقة الزمنية و`YER` أو عملة عالمية أخرى والقالب، وحالات الانتظار وإعادة الإرسال والتحقق والاكتمال والخطأ، مع تحقق بصري فعلي مكتبي وهاتف 390×844 بالاتجاهين دون تمرير أفقي.
- [x] إضافة اختبارات PRE_AUTH، وحدود HTTP وRate Limit وأخطاء الرمز ومخططات OpenAPI، واختبار تكامل MariaDB يثبت عدم الإنشاء قبل التحقق والتزامن وإعادة التشغيل والدور والصلاحيات والدليل ومنع تعداد الهوية والتعافي من فشل البريد.
- [x] حزمة من 137 حالة اختبار في 43 ملفًا: نجحت 97 محليًا وتجاوزت Vitest الحالات الـ40 المعتمدة على قاعدة البيانات لعدم ضبط `RUN_DB_TESTS`، مع نجاح Prisma وTypeScript والعقد المولد واختبارات API والواجهة والبنية التحتية والبناء.

## المرحلة 37 — استكمال قواميس الواجهة وربط أكواد الأعمال

- [x] نقل النصوص المرئية في جميع الصفحات التشغيلية إلى قواميس منفصلة؛ أضيفت 800 رسالة صفحة عربية ومقابل إنجليزي مع تصحيحات بشرية للمصطلحات المحاسبية، ولم يعد مصدر TS/TSX غير الاختباري يحتوي نصًا عربيًا خارج مجلد القواميس.
- [x] فصل قواميس الحالات وأخطاء الأعمال والتحقق وعناوين CSV وإجراءات وكيانات التدقيق وأنواع أحداث الأمان؛ تحل الواجهة `reason` ثم `code` إلى مفتاح ترجمة وتعود إلى رسالة افتراضية بدل عرض نص الخادم.
- [x] جعل سجل اللغات المصدر الوحيد للاتجاه و`Intl` ومبدلات اللغة وخيارات التسجيل، وربط تنسيق الأرقام والتواريخ والشهور والتقارير باللغة الفعالة بدل تثبيت `ar-SA`.
- [x] نقل اسم المنتج والاسم المختصر وحرف العلامة إلى القواميس مع بقاء متغيرات `VITE_APP_*` قادرة على تجاوزها، وتحديث عنوان المستند عند تبديل اللغة.
- [x] استخدام `localizedReferenceName()` في الصفحات بدل `nameAr` المباشر، مع أولوية `names[locale]` المستقبلية ثم `nameEn` والعودة إلى العربية، وتوثيق استجابات العملات والمرجعيات والتقارير التي لا تزال ترسل اسمًا عربيًا فقط.
- [x] إضافة بوابة `npm run i18n:check` المبنية على Babel إلى مهمتي CI؛ تفشل عند وجود العربية خارج القواميس أو نص JSX مرئي صلب أو لغة `Intl` مثبتة، مع اختبارات تكافؤ المفاتيح وعناصر الاستيفاء وربط أكواد الأعمال بالقاموسين.
- [x] فحص الدخول والتسجيل الفعليين بالعربية والإنجليزية عند 390×844 و1280×900؛ تبدل `lang` و`dir` وعنوان الصفحة، ظهر `YER` باسمه المناسب، كان الهاتف عمودًا واحدًا والمكتب عمودين، ولم يظهر تمرير أفقي أو مفتاح ناقص. استُخدم خادم خيارات مؤقت للقراءة فقط لأن MariaDB المحلية غير عاملة، دون إرسال نموذج أو إنشاء بيانات.
- [x] توثيق إضافة لغة وعقد أسماء المرجعيات والبوابات وقائمة المراجعة في `docs/localization.md`، وإغلاق المرحلة الرابعة من `docs/product-roadmap.md` دون ترحيل قاعدة بيانات جديد.
- [x] نجاح فحص القواميس والعقد المولد وTypeScript وبناء API وReact/Vite؛ نجحت 67 حالة API غير معتمدة على قاعدة البيانات و20 حالة واجهة و13 حالة بنية تحتية، وتجاوزت 40 حالة تكامل لغياب قاعدة محلية عاملة.

## المرحلة 38 — نظام الواجهات والفحص البصري الشامل

- [x] توحيد عناوين 16 صفحة تشغيلية عبر `PageHeader`، وإضافة `ErrorState` و`TableRegion` وتوسيع زر الواجهة بحيث يحافظ على الأصناف ويستخدم `type="button"` افتراضيًا.
- [x] تحويل 31 حاوية جدول إلى مناطق مسماة قابلة للتركيز والتمرير الأفقي، وإعلان جميع رسائل `error-panel` و`form-error` كتنبيهات، وإضافة تسميات صريحة لحقول الأدوات غير المحاطة بـ`label`.
- [x] تحسين `Modal` بمعرفات فريدة وقفل تمرير الخلفية والإغلاق بـ`Escape` وحبس التركيز واستعادته، وإضافة رابط تجاوز للمحتوى ومعالجة `focus-visible` و`prefers-reduced-motion`.
- [x] استبدال خصائص الاتجاه المادية في الغلاف والمحاذاة بخصائص منطقية، وتقييد المحتوى برمز مشترك، ومنع عناصر Grid والعناوين الطويلة من تمديد الهاتف أو تقليص زر القائمة.
- [x] إضافة `scripts/check-web-ui.mjs` وبوابة `npm run ui:check` إلى مهمتي CI؛ تفشل عند فقد تسميات الحقول أو أنواع أزرار النماذج أو دلالات الجداول والأخطاء أو عقود CSS الأساسية.
- [x] إضافة خادم بيانات بصري حتمي للقراءة فقط، وتوثيق طريقة التشغيل والمصفوفة و16 لقطة مرجعية في `docs/visual-qa.md`.
- [x] فحص 18 شاشة × لغتين × 4 عروض (390/768/1440/1920) = 144 حالة؛ لم يبق تمدد للمستند أو تداخل عناوين أو أهداف تفاعل صغيرة أو أعطال واجهة، ونجح اختبار فتح النافذة وإغلاقها واستعادة التركيز.
- [x] تحديث التدقيق إلى 19 شاشة × لغتين × 4 عروض = 152 حالة، وإضافة 16 حالة مركزة لنافذتي فواتير المبيعات والمشتريات؛ نجحت الحالات الـ168 دون تمدد للمستند أو `main` أو قص حقول الفاتورة.
- [x] إزالة إرشادات `SEED_ADMIN_PASSWORD` وادعاء الحماية العام وهوية حساب التطوير من الدخول، وتنقية نصوص التسجيل والإعدادات من تفاصيل التنفيذ مع الحفاظ على الآثار الوظيفية المهمة.
- [x] تثبيت البحث والتبويبات كوحدات غير مفككة، وإعادة توزيع بنود الفاتورة في صفين متجاوبين، وتوسيع `ui:check` لمنع عودة النصوص التشغيلية وعقود التمدد، وتوثيق السياسة في `docs/UI_CONTENT_RESPONSIVE_POLICY_AR.md`.

## المرحلة 39 — E2E وبوابات الإصدار والإطلاق التدريجي

- [x] إضافة Playwright 1.62.1 وChromium وإعداد حتمي بخادم API واحد يقدم أصول الواجهة المبنية على منفذ اختبار مستقل.
- [x] إضافة رحلة E2E تبدأ بالتسجيل والتحقق عبر رابط بريد ملتقط في JSONL مؤقت، ثم الدخول وتبديل العربية/الإنجليزية والتحقق من `lang` و`dir`.
- [x] إنشاء عملة `XQZ` معزولة وسعر صرف مؤرخ من الواجهة، ثم سنة مالية واختيار الفترة المطابقة وإنشاء أول قيد يدوي متوازن والتحقق من استجابة 201 والمسودة.
- [x] منع `REGISTRATION_EMAIL_CAPTURE_PATH` في الإنتاج واختبار الرفض، مع اختبار مستقل لصيغة الالتقاط وعدم إدخال كلمة المرور أو جسم الطلب فيها.
- [x] إضافة `migrate status` وE2E ورفع تقارير الفشل ونسخة/استعادة معزولة إلى بوابتي MariaDB 10.11 وMySQL 8.4.
- [x] إضافة تجربة رجوع فعلية تختبر تبديل الإصدار السابق ذريًا، وتثبت استعادة الإصدار الأصلي عندما يفشل هدف الرجوع في الجاهزية.
- [x] كشف اختبار قاعدة البيانات عيب تمرير `adminPassword` إلى مخطط الإدخال المُحضّر الصارم؛ فُصل السر قبل التحقق وأضيف اختبار ارتداد، ثم نجح اختبار تجهيز شركتين وإعادة التشغيل.
- [x] تطبيق 26 Migration من قاعدة فارغة على MariaDB 10.4 معزولة، ونجاح E2E، وإنشاء نسخة مشفرة واستعادة 43 جدولًا و26 Migration إلى قاعدة فارغة.
- [x] توثيق خطة إطلاق تبدأ بقاطع `SELF_REGISTRATION_ENABLED=false`، ثم تفعيل مراقب، ومؤشرات البريد والتجهيز و429، وخطة الإيقاف والرجوع غير الهادم.
- [x] لا يوجد Migration جديد في هذه المرحلة؛ التغييرات محصورة في بوابات الاختبار والتشغيل وإصلاح إعداد مدخل موجود.

## المرحلة 40 — Transactional Outbox لبريد التسجيل

- [x] إضافة `OutboxEvent` مملوك لبنية التطبيق عبر الترحيل `20260822100000_transactional_outbox` بعقد versioned وحالات claim/processing/processed/failed وlease ومحاولات ومواعيد إتاحة، من دون FK شركة لأن التسجيل يسبق إنشاء المستأجر.
- [x] إلحاق `RegistrationVerificationRequested` ذريًا داخل معاملة بدء التسجيل أو إعادة الإرسال، مع `aggregateId` عام و`deliveryGeneration` فقط في الحمولة؛ لا بريد ولا كلمة مرور ولا رمز ولا Prisma record.
- [x] اشتقاق رمز opaque ثابت لكل حدث عبر HMAC بمفتاح إنتاج مستقل ثم تخزين SHA-256 فقط؛ retry يعيد الرابط الصالح نفسه، بينما resend يزيد الجيل ويدور الرمز فورًا.
- [x] إنشاء عامل آمن لعدة عمليات باستخدام compare-and-set وlease قابل للاستعادة، ومنع إعادة الحدث داخل الدفعة ذاتها، وmax attempts وfull-jitter exponential backoff وdeadline للمحول.
- [x] دمج بدء العامل وإيقافه مع دورة خادم HTTP وPrisma، وإضافة سجلات منظمة للـlag وretry وdead letter وفقد lease ولقطة تراكم دورية، مع حجب البريد والرابط عن محول التطوير الافتراضي.
- [x] تنظيف دفعـي للأحداث `PROCESSED` بعد مدة الاحتفاظ والإبقاء على `FAILED` للتحقيق، وتوثيق استعلام التراكم وإجراء التعافي عبر resend بدل تعديل الصف يدويًا.
- [x] Backfill تلقائي لطلبات `PENDING_EMAIL` السابقة؛ نجحت 27 Migration من قاعدة فارغة ومحاكاة ترقية من 26 Migration مع طلب معلق، وأثبت الفحص أن الحمولة لا تحمل البريد أو الرمز.
- [x] سبع حالات تكامل MariaDB تثبت الالتزام قبل البريد، والـrollback عند تعذر كتابة Outbox، والتعافي من انقطاع المزود مع الرمز نفسه، ودوران resend، وclaim متزامن مع lease منتهٍ، وdead letter، وسياسة التنظيف؛ نجحت حزمة API كاملة 118/118 مع اختبار إيقاف العامل النائم.

## المرحلة 41 — النشر الإنتاجي التلقائي

- [x] إضافة مهمة نشر لا تعمل إلا على `push` إلى `main` وبعد نجاح بوابتي MariaDB وMySQL.
- [x] تنزيل Artifact من تشغيل CI نفسه والتحقق من SHA-256 قبل الرفع.
- [x] تثبيت هوية SSH مخصصة وبصمة خادم iFastNet بدل `ssh-keyscan` وقت النشر.
- [x] تمرير سر تشفير النسخ عبر قناة SSH القياسية دون تخزينه في Artifact أو سطر أوامر الخادم.
- [x] إنشاء نسخة مشفرة قبل كل ترحيل إنتاجي.
- [x] تنفيذ Migration وSeed على الإصدار المرشح قبل تبديل `current`.
- [x] الاحتفاظ بالتبديل الذري وفحص الجاهزية وبصمة الواجهة والرجوع الآلي الموجودين.
- [x] توثيق سري GitHub المطلوبين وإجراء الطوارئ اليدوي.

## المرحلة 42 — التوليد الذري لرموز العملاء والموردين

- [x] إضافة `MasterDataCodeSequence` مملوك لبنية التطبيق، ومعزول حسب الشركة ونوع الكيان، عبر الترحيل `20260822150000_auto_master_data_codes`.
- [x] الحفاظ على جميع الرموز السابقة، وتهيئة نقطة بداية آمنة لكل تسلسل من البيانات الموجودة دون استخدام `MAX()+1` في مسار التشغيل.
- [x] إنشاء خدمة حجز ذرية تعيد `CUS-000001` للعملاء و`SUP-000001` للموردين داخل معاملة إنشاء الكيان نفسها.
- [x] إزالة `code` من مدخلات إنشاء وتعديل العملاء والموردين، وجعله `readOnly` في OpenAPI ورفض إرساله يدويًا بعقد API الصارم.
- [x] تحديث الواجهة العربية والإنجليزية لإظهار أن الرمز يولد تلقائيًا، وعرضه للقراءة فقط عند التعديل.
- [x] توثيق القرار في `MASTER_DATA_CODE_POLICY_AR.md` وربطه بالضوابط وخريطة السياقات وقائمة مراجعة التغيير وتعليمات الوكلاء.
- [x] اختبار 12 إنشاء عميل متزامنًا و12 إنشاء مورد متزامنًا على MariaDB الفعلية، مع تفرد الرموز وصحتها وعدم قابليتها للتعديل.
- [x] تطبيق 28 Migration من الصفر وتشغيل Seed وDemo Seed، ثم نجاح حزمة API كاملة 122/122 على قاعدة MariaDB 10.4 معزولة وحذف قاعدة التحقق بعد النجاح.

## المرحلة 43 — تعميم الرموز واستعادة كلمة المرور

- [x] توسيع `MasterDataCodeSequence` ليولد ذريًا رموز مراكز التكلفة `CC-`، وحسابات الصندوق/البنك `CB-`، وطرق الدفع الخاصة `PM-{companyId}-`، ومعدلات الضريبة `TAX-`، والأدوار المخصصة `ROL-`، إضافة إلى العملاء والموردين.
- [x] إزالة `code` من عقود الإنشاء والتعديل لهذه الأنواع ومن نماذج الواجهة، مع إبقائه للقراءة فقط وثابتًا بعد الإنشاء.
- [x] إبقاء رمز الحساب المحاسبي ورمز العملة يدويين لأنهما دلاليان، وإبقاء رموز الصلاحيات والأدوار النظامية والقوالب مملوكة للـSeed/Migration.
- [x] إضافة `PasswordResetRequest` وحالات الاستخدام العامة لبدء الاستعادة وإكمالها، باستجابة تمنع تعداد الحسابات ورمز أحادي الاستخدام لا يخزن خامًا.
- [x] إلحاق `PasswordResetRequested` مع الطلب في معاملة واحدة، وإرسال البريد بعد الالتزام عبر عامل Outbox القائم، مع HMAC domain separator مستقل ورابط داخل URL Fragment.
- [x] جعل طلب جديد يلغي الروابط المعلقة الأقدم، وجعل الاستهلاك conditional تحت التزامن، وإلغاء جميع الجلسات وتصفير القفل وتسجيل حدث أمان عالي الخطورة بعد النجاح.
- [x] إضافة PRE_AUTH CSRF وRate Limit مستقل، ومتغيرات تشغيل وقاطع ميزة ومتطلبات Fail-fast للبريد ومفتاح الرمز في الإنتاج.
- [x] ترقية عقد OpenAPI إلى `1.6.0` وإضافة مساري الاستعادة وجعل الرموز المولدة `readOnly` مع 12 Request Guard مولدة و150 عملية HTTP متطابقة.
- [x] توثيق سياسة الرموز والاستثناءات وسياسة أمان استعادة كلمة المرور وربطهما بالحوكمة وقائمة مراجعة التغيير ودليلي التشغيل والنشر.
- [x] تطبيق 29 Migration من قاعدة فارغة على MariaDB 10.4.32 وتشغيل Seed وDemo Seed، ثم نجاح 127/127 اختبار API فعلي وحذف قاعدة التحقق المعزولة بعد النجاح.
- [x] نجاح TypeScript، و20/20 اختبار واجهة، و17/17 اختبار بنية تحتية، وبناء Vite للإنتاج، وبوابتي i18n وUI.

## المرحلة 44 — أتمتة مصفوفة الواجهة المتجاوبة

- [x] إضافة `playwright.visual.config.ts` مستقل بأربعة مشاريع Chromium لعروض 390 و768 و1440 و1920، وخادمي Vite وبيانات بصرية حتمية يعملان تلقائيًا دون قاعدة بيانات أو إرسال بريد.
- [x] إضافة `tests/visual/responsive-ui.spec.ts` لتغطية 19 شاشة بالعربية والإنجليزية، ونافذتي إنشاء فواتير المبيعات والمشتريات؛ تمثل 16 حالة Playwright و168 حالة واجهة فعلية.
- [x] قياس تمدد المستند و`main` ومساحة العمل وحدود عناصر التفاعل، ومنع مفاتيح الترجمة والنصوص التشغيلية، والتحقق من `lang/dir` وفراغ هوية الدخول وعدم التفاف البحث والتبويبات.
- [x] التحقق من عنوان النافذة الفريد، وقفل الخلفية، وبقاء التركيز داخلها، واحتواء النموذج و`fieldset` ومحرر البند، والإغلاق بـ`Escape` واستعادة التركيز إلى زر الفتح.
- [x] كشف الفحص المرئي المباشر تضخم صندوق البحث على الهاتف بسبب انتقال `flex-basis: 260px` إلى المحور العمودي؛ صُفّر الأساس في التخطيط العمودي وأضيف حد آلي لارتفاع صندوق البحث وعقد ساكن في `ui:check`.
- [x] إضافة `npm run visual-qa:test` إلى مهمة MySQL الحاجبة للإصدار والنشر، وحفظ تقرير HTML ولقطة وفيديو وTrace عند الفشل.
- [x] نجاح الحالات الـ16/16 محليًا في 2.3 دقيقة، مع تحميل مساحة العمل مرة واحدة والتنقل بين أقسامها من القائمة لتقليل اتصالات الاختبار ومنع استنزاف منافذ النظام.
- [x] تحديث `README.md` وسياسة محتوى الواجهة والتجاوب ودليل الفحص البصري وخارطة التطوير؛ لا يوجد Migration أو تغيير في المجال أو البيانات في هذه المرحلة.

## المرحلة 45 — ملكية دفتر الأستاذ والتزامن المالي المركزي

- [x] إنشاء `PostingEngine` داخل Core Accounting بوصفه المنفذ الوحيد لإنشاء القيود المرحلة وعكسها وتغيير حالات المستندات إلى `POSTED/REVERSED`، مع إبقاء إنشاء وتعديل مسودة القيد اليدوي داخل السياق المالك.
- [x] ترحيل القيود اليدوية وسندات القبض والصرف وفواتير المبيعات والمشتريات وإشعاراتها إلى Posting Plans مركزية، من دون تغيير مسارات HTTP أو أجسام الطلب والنجاح.
- [x] إنشاء `TransactionExecutor` موحد بحد ثلاث محاولات وdeadline مشترك وfull-jitter exponential backoff و`maxWait/timeout`، وتصنيف `P2034/1213/1205` مع عدم إعادة `P2002` أو أخطاء الأعمال.
- [x] توحيد Idempotency للأوامر الحساسة ونقل آخر حلقات retry من المدفوعات وحجز الأرقام والتسجيل إلى Infrastructure واحدة؛ يعاد تنفيذ transaction callback كاملًا ولا يوجد نوم داخل المعاملة.
- [x] تطبيق ترتيب أقفال ثابت: Idempotency ثم الفترة ثم المستند ثم أهداف التسوية تصاعديًا ثم قيود اليومية، ومشاركة `FiscalService` لقفل الفترة نفسه لمنع سباق post-vs-close.
- [x] إعادة التحقق من حالة فاتورة المصدر والرصيد بعد قفل سطر الذمة، ومنع اجتماع عكس الفاتورة مع قبض/دفع مرحل أو تجاوز مجموع التسويات للـOutstanding.
- [x] إضافة استجابتي `503 CONCURRENCY_RETRY_EXHAUSTED` و`504 REQUEST_DEADLINE_EXCEEDED` إلى التطبيق وOpenAPI 1.7.0، مع رسائل آمنة و`requestId` وسجلات منظمة للمحاولة والمدة والتصنيف والعدادات.
- [x] إضافة اختبارات وحدة للمصنف والـjitter والحد والـdeadline، وحارس معماري يمنع الكتابة المباشرة إلى Ledger أو إعادة حلقات `P2034` داخل الخدمات المالية.
- [x] إضافة اختبارات قاعدة بيانات لـpost-vs-close وreverse-vs-receipt/payment وتسويتين متزامنتين على سطر AR/AP واحد، مع إثبات عدم الأثر الجزئي والتوازن والـIdempotency.
- [x] تطبيق الترحيلات الـ29 وSeed وDemo Seed من الصفر ثم نجاح 140/140 اختبار API في 43 ملفًا على MariaDB 10.4.32 وعلى MySQL 8.4.11، ونجاح TypeScript وOpenAPI/Redocly و20/20 اختبار واجهة و17/17 اختبار بنية تحتية وبناء API وVite.
- [x] لا يوجد Migration جديد؛ المرحلة تعيد تنظيم حدود الملكية والمعاملات وتحافظ على Strong Consistency داخل Modular Monolith.

## المرحلة 46 — مهلة الطلب والقياسات والتنبيهات التشغيلية

- [x] إنشاء `RequestExecutionContext` موحد عبر `AsyncLocalStorage` يبدأ قبل تحليل JSON ويحمل `requestId` و`requestClass` و`deadlineAt` و`AbortSignal` إلى الأعمال غير المتزامنة.
- [x] جعل `TransactionExecutor` يختار أصغر Deadline بين الطلب والخيار المطلق والميزانية المحلية، ويخفض `maxWait/timeout` إلى المتبقي، ويلغي backoff والمحاولات التالية عند انتهاء المهلة أو انقطاع العميل.
- [x] منع إرسال نجاح بعد Deadline: يعاد `504 REQUEST_DEADLINE_EXCEEDED` مرة واحدة، وتُقمع الاستجابة المتأخرة؛ ووُثّق أن 504 لا يثبت غياب Commit ذري عند حافة المهلة، لذا يعاد التحقق والمحاولة بالمفتاح نفسه.
- [x] إيقاف الخطوات الجديدة عند انقطاع العميل دون قطع معاملة جارية قطعًا قسريًا أو تعريض Strong Consistency لأثر جزئي، وربط انتظار التسجيل وإحلال Idempotency بالإشارة المشتركة.
- [x] ضبط `requestTimeout=70s` و`headersTimeout=10s` و`keepAliveTimeout=5s` صراحةً، مع Deadlines للقراءة 10s والكتابة 15s والتسجيل 65s والتحقق المبكر من الهرم.
- [x] إنشاء `MetricsRegistry` داخلي يدعم Counter/Gauge/Histogram بصيغة Prometheus، مع تسميات bounded لا تحتوي companyId أو requestId أو Idempotency key أو payload ماليًا.
- [x] قياس مدة المعاملات وdeadlock وlock-wait وwrite conflicts وretry/exhaustion وoptimistic version conflicts وrequest deadlines وclient disconnect وOutbox lag/retry/dead-letter.
- [x] إضافة `/metrics` اختياريًا مع Bearer مستقل إلزامي في الإنتاج، ورفض الإعداد غير الآمن مبكرًا، وتحديث OpenAPI إلى 1.8.0 مع مطابقة المسار والمخرجات.
- [x] إضافة خمس قواعد تنبيه مدمجة قابلة للضبط: نسب deadlock وretry exhaustion، وعدد deadlines، وOutbox lag وdead-letter، مع Active Gauge وسجلات firing/resolved وcooldown.
- [x] إضافة اختبارات deadline المطلق، انقطاع العميل، قمع النجاح المتأخر، مهلات Node، metrics/alerts، حماية Bearer، وحراس معمارية، وProbe محدود من 16 معاملة متزامنة مقاسة.
- [x] نجاح 156/156 اختبار API في 47 ملفًا على MariaDB 10.4.32 وMySQL 8.4.11 من قاعدتين فارغتين بعد الترحيلات الـ29 وSeed وDemo Seed، ونجاح TypeScript وبناء API/Vite و20/20 اختبار واجهة و17/17 اختبار بنية تحتية وOpenAPI/Redocly.
- [x] تحديث نماذج البيئة وREADME ودليلي الإنتاج وPassenger وسياسة التزامن ودليل تشغيل مستقل للمهلات والقياسات والاستجابة للحوادث؛ لا يوجد Migration أو تغيير في مخطط البيانات.

## المرحلة 47 — فصل هوية تسويات الذمم عن Ledger

- [x] إضافة `ReceivableItem` و`PayableItem` بهوية مستقرة وعزل شركة وأصل ورصيد وحالة `OPEN/PARTIAL/SETTLED/REVERSED` ونسخة متفائلة.
- [x] إنشاء `ReceivableSettlementPort` و`PayableSettlementPort`؛ تستخدمهما Treasury للتحقق والتطبيق والاستعادة داخل معاملة `PostingEngine` بدل قراءة أو تعديل `JournalLine` كعقد بين المجالات.
- [x] إنشاء العنصر مع ترحيل فاتورة المصدر، وتطبيق وعكس القبض والدفع والإشعارات داخل المعاملة نفسها، ومنع عكس المصدر بعد التسوية.
- [x] استبدال `targetJournalLineId` في Prisma وOpenAPI 1.9.0 والخدمات والواجهة بـ`receivableItemId/payableItemId`، مع إبقاء رابط السطر الداخلي أثرًا غير عام.
- [x] إضافة الترحيل `20260822230000_receivable_payable_items` مع backfill للأرصدة والتخصيصات وحواجز تفشل عند بيانات غير قابلة للمطابقة أو رصيد تاريخي غير صالح.
- [x] نجاح Migration من قاعدة فارغة وترقية مخطط سابق ببيانات فعلية على MariaDB 10.4.32 وMySQL 8.4.11؛ نُقلت تخصيصات جزئية واختفت أعمدة الهدف القديمة.
- [x] إضافة حراس معمارية وعزل شركة واختبارات عكس القبض والدفع والإشعارات والنسخ والحالات وover-allocation وreverse-vs-settlement.
- [x] توثيق ملكية البيانات وترتيب الأقفال وخطة النشر أحادية النسخة والرجوع باستعادة النسخة الاحتياطية في `AR_AP_SETTLEMENT_ITEMS_AR.md`.

## المرحلة 48 — ملكية Tax والحاسبة الضريبية الموحدة

- [x] إنشاء وحدة `Tax` مالكة وحيدة لـ`TaxRate` وسياسات حسابات ضريبة المخرجات والمدخلات، وإتاحة `TaxQuotePort` متزامنًا للمبيعات والمشتريات.
- [x] نقل CRUD نسب الضرائب إلى `TaxService` و`tax-router` مع إبقاء المسارات العامة الحالية بلا كسر، وعزل كل قراءة وكتابة بالشركة.
- [x] إضافة نسخة متفائلة `version` وتحديث شرطي داخل `TransactionExecutor`؛ ينجح تحديث واحد فقط من تحديثين متنافسين ويعاد `409 VERSION_CONFLICT` للآخر.
- [x] توحيد حساب أسطر فواتير البيع والشراء في حاسبة Decimal واحدة بأربع منازل وتقريب `HALF_UP`، مع حساب الإجمالي الأساسي من صافي الأساس وضريبته بعد التقريب المنفصل كي يطابق تفاصيل القيد.
- [x] حذف حاسبتي المبيعات والمشتريات المكررتين ومنع Prisma CRUD المباشر على `TaxRate` خارج الوحدة بحواجز معمارية.
- [x] إضافة اختبارات وحدة وتكامل لعزل الشركات وتصنيف حسابات الإدخال والإخراج والتزامن، ونجاح 168/168 اختبار API على MariaDB 10.4.32 وMySQL 8.4.11.
- [x] إضافة الترحيل `20260822233000_tax_context_ownership`، ونجاح تطبيق الترحيلات الـ31 من قاعدة فارغة وترقية مخطط سابق يحتفظ بصفوف الضرائب على المحركين.
- [x] تحديث OpenAPI إلى 1.10.0 وعقود الواجهة لإرسال `version`، وتحديث خريطة السياقات والحوكمة ووثيقة النشر والرجوع `TAX_CONTEXT_OWNERSHIP_AR.md`.

## المرحلة 49 — ملكية Treasury وأدوات الحركة النقدية

- [x] إنشاء وحدة `Treasury` مالكة وحيدة لـ`CashBankAccount` و`PaymentMethod`، ونقل CRUD إلى `TreasuryService` و`treasury-router` مع إبقاء المسارات العامة بلا كسر.
- [x] حذف CRUD والمسلسلات المكررة من مرجعي العملاء والموردين، وإضافة حارس يمنع Prisma CRUD المباشر على نموذجي Treasury خارج الوحدة، مع استثناء Seed الإعدادي الموثق.
- [x] إتاحة `TreasuryInstrumentPort` للقبض والدفع كي يتحققا من الأداة وحساب الترحيل داخل المعاملة نفسها من دون دمج قواعد AR وAP المختلفة أو تغيير `PostingEngine` وعقود عناصر الذمم.
- [x] إضافة نسخة متفائلة `version` للحسابات النقدية وطرق الدفع الخاصة، وتحديث شرطي داخل `TransactionExecutor`؛ أثبت اختبارا السباق أن استجابة واحدة فقط تنجح والأخرى تعيد `409 VERSION_CONFLICT`.
- [x] إضافة اختبارات وحدة وتكامل لعزل الشركات، ومنع ربط حساب ترحيل تابع لشركة أخرى، وإخفاء الأرقام البنكية، وإبقاء طرق الدفع العامة للقراءة فقط.
- [x] إضافة الترحيل `20260822234500_treasury_context_ownership`، ونجاح تطبيق الترحيلات الـ32 من قاعدة فارغة وترقية مخطط سابق احتفظ بحسابين نقديين وأربع طرق دفع وأعطاها النسخة صفر على MariaDB 10.4.32 وMySQL 8.4.11.
- [x] تحديث OpenAPI إلى 1.11.0 وعقود الواجهة لإرسال `version` في التعديل والتعطيل، وتوثيق الحدود والتوافق والنشر والرجوع في `TREASURY_CONTEXT_OWNERSHIP_AR.md`.
- [x] نجاح 178/178 اختبار API في 51 ملفًا على المحركين، و20/20 اختبار واجهة، و17/17 اختبار بنية تحتية، وفحوص TypeScript والبناء وOpenAPI وRedocly وi18n وعقد الواجهة.

## المرحلة 50 — OpenAPI كعقد تنفيذي شامل

- [x] ترقية OpenAPI إلى `1.12.0` وجعل المولد يكتشف العمليات تلقائيًا؛ يغطي الآن 151 عملية و77 جسم طلب JSON و1059 جسم استجابة JSON مرتبطًا بالعملية ورمز الحالة.
- [x] توسيع المولد الحتمي ليدعم `$ref` و`allOf/oneOf/anyOf/not` والكائنات والمصفوفات والقيود والتعدادات و`readOnly/writeOnly` والتواريخ والبريد وUUID و`x-trim`، مع فصل وضع الطلب عن الاستجابة.
- [x] تحويل معرّفات BIGINT الموجبة إلى `bigint` عند حد الطلب المولد مع إبقائها نصوصًا في الاستجابة، والحفاظ على DECIMAL والتواريخ كنصوص نقل دقيقة، وتنظيف خصائص `undefined` الاختيارية.
- [x] استبدال جميع مخططات أجسام الطلب اليدوية في Routers بحراس مولدة، وإضافة بوابة معمارية تقارن اعتماد العمليات الـ77 تلقائيًا وتمنع عودة مخطط Zod موازٍ.
- [x] إضافة سجل عمليات ومسارات ومخططات استجابة وأنواع aliases مولدة، ومدقق استجابات في التطوير والاختبار يرفض الجسم المخالف أو رمز الحالة غير المعلن ويسجل تشخيصًا آمنًا بلا قيم حساسة.
- [x] تصحيح الانحرافات التي كشفها الاختبار الفعلي من دون كسر سلوك API: تسجيل الدخول والشركات والجلسات، `PaginationMeta.total`، الصحة و503، نتائج أوامر الفواتير، لوحة المؤشرات ومركز التكلفة، 422 للضرائب، وXOR الذي يقبل الطرف المقابل `null`، مع إبقاء غلاف الخطأ القديم متوافقًا مؤقتًا.
- [x] إضافة اختبارات حتمية وتقادم وتحويلات وقيود سلبية واستجابات وحالات غير معلنة، وتوثيق السياسة الحاكمة في `OPENAPI_EXECUTABLE_CONTRACTS_AR.md` وتحديث قائمة المراجعة والضوابط.
- [x] نجاح 186/186 اختبار API في 52 ملفًا على MariaDB 10.4.32 وMySQL 8.4.11 بعد 32 ترحيلًا وSeed وDemo Seed من قاعدتين فارغتين، و20/20 اختبار واجهة و18/18 اختبار بنية تحتية وTypeScript والبناء وRedocly وi18n وعقد الواجهة.
- [x] لا يوجد Migration جديد؛ المرحلة توحد عقد HTTP والتوليد والحوكمة وتحافظ على المسارات والسلوك العام.

## المرحلة 51 — مركز الاستيراد الجماعي والجاهزية التشغيلية

- [x] إضافة سياق داعم `Data Import` يملك `DataImportBatch` فقط، مع توثيق الملكية ودورة المعاينة والاعتماد في `DATA_IMPORT_CONTEXT_AR.md` وخريطة السياقات.
- [x] دعم قوالب CSV UTF-8 وXLSX للعملاء والموردين وفواتير البيع والشراء؛ يستخدم الملف مفاتيح أعمدة ثابتة ورموزًا مرجعية بدل معرفات داخلية.
- [x] تحليل الملف قبل فتح المعاملة، وحد 512 كيلوبايت و500 صف و100 فاتورة، وفحص ZIP المركزي وحد الحجم بعد فك الضغط، ورفض التشفير والصيغ والصفوف المخفية داخل XLSX.
- [x] إنشاء معاينة قصيرة العمر تحفظ SHA-256 والأعداد فقط ولا تحفظ الملف أو صفوفه، ثم إلزام العميل بإعادة الملف نفسه والتحقق من بصمته والنوع والشركة والمستخدم والانتهاء.
- [x] اعتماد ذري كامل بمفتاح Idempotency وإعادة تحقق داخل المعاملة؛ تنشأ الفواتير كـ`DRAFT` فقط ولا ينشئ الاستيراد قيدًا أو تسوية أو أثر Ledger.
- [x] إنشاء السجلات عبر منافذ مالكي العملاء والموردين وفواتير البيع والشراء، وحل الضرائب عبر `TaxQuotePort` بدل Prisma عابر للسياق، مع Audit Log وحدث `DataImportCommitted` في Transactional Outbox.
- [x] إضافة صلاحيتين `data_imports.view` و`data_imports.execute` مع اشتراط صلاحية إنشاء النوع المستهدف؛ سجل الدفعات معزول حسب الشركة، والاعتماد مقيد بمنشئ المعاينة.
- [x] إضافة واجهة عربية وإنجليزية متجاوبة لتنزيل القالب واختيار الملف والمعاينة والأخطاء الصفية والاعتماد والسجل، من دون عرض enums أو معرفات فنية للمستخدم.
- [x] إغلاق فجوة جاهزية الضرائب: تبقى النسبة غير الجاهزة مرئية لكن معطلة مع سبب واضح، ويمنع المنفذ استخدامها؛ وإظهار رقم الفاتورة واسم العميل وتاريخ الاستحقاق في تفاصيل تخصيص القبض بدل معرف عنصر الذمة الخام.
- [x] ترقية OpenAPI إلى `1.13.0`: 155 عملية و79 جسم طلب و1083 جسم استجابة مولدة، مع مطابقة المسارات وRedocly.
- [x] إضافة اختبارات CSV/XLSX السلبية والصيغ الخطرة، والعزل، وتعديل الملف، والاعتماد الجزئي، وIdempotency، وسباق اعتمادين، وإنشاء الأنواع الأربعة كمسودات؛ نجحت حزمة API 194/194 محليًا على MariaDB 10.4.32 بعد 33 ترحيلًا من قاعدة فارغة وSeed وDemo Seed.

## المرحلة 52 — تدقيق اعتماد بيئة الإنتاج

- [x] إثبات استقلال النطاق والتطبيق وقاعدة `doralash_mcapprod`، ونجاح TLS 1.3 والشهادة والواجهة و`/live` و`/health` و`/ready` وقاعدة البيانات في 24 أغسطس 2026.
- [x] إثبات نجاح النسخة المشفرة السابقة للترحيل وتطبيق 33 Migration وتفعيل الإصدار `0.1.0-fa9a63665a91` من سجل تشغيل GitHub Actions رقم 74.
- [x] تدقيق بيئة GitHub وأسماء الأسرار دون قراءة قيمها، وتدقيق ضوابط HTTP والكوكي وCSP/HSTS/CORS وحالة التسجيل والقياسات.
- [x] فرض `308` مُدار في Apache/cPanel إلى النطاق المعياري مع حفظ المسار والاستعلام، ونسخ `.htaccess` واستعادته ذريًا عند فشل التفعيل، وإضافة حارس نشر يرفض HTTP المباشر أو `Location` غير المطابق.
- [x] تقييد بيئة `production` بفرع `main` وحده، وإعداد Classic Branch Protection تفرض PR وتحديث الفرع وبوابتي CI وحل المحادثات ومنع التجاوز والحذف وForce Push.
- [x] إضافة حارس منشأ قبل أسرار الإنتاج يرفض Force Push أو أي التزام لا يطابق PR واحدًا مدمجًا إلى `main`؛ وهو ضابط تعويضي ضد النشر المباشر لا ضد تعديل Workflow بواسطة مسؤول يملك Push.
- [ ] تفعيل إنفاذ قاعدة `main`؛ تعرضها GitHub حاليًا `Not enforced` لأن المستودع الشخصي خاص على GitHub Free، ويحتاج GitHub Pro أو النقل إلى مؤسسة Team/Enterprise.
- [x] نقل `CPANEL_SSH_PRIVATE_KEY` و`BACKUP_ENCRYPTION_PASSPHRASE` دون كشفهما إلى بيئة `production` عبر Fine-grained PAT مؤقت مقيد بالمستودع و`Environments: write`؛ تحقق تشغيل النقل رقم 1 من وجود السجلين.
- [x] إثبات تشغيل القبول رقم 84 بأسرار Environment ذات الأولوية، ثم حذف نسختيهما والسر المؤقت من Repository secrets وإلغاء PAT؛ وأثبت التشغيل النهائي رقم 87 نجاح النشر بلا أي fallback أوسع.
- [x] فصل مستخدم تشغيل قاعدة البيانات DML عن مستخدم الترحيل DDL وإثبات السياسة والإنتاج في تشغيل القبول 108.
- [x] تفعيل `/metrics` برمز مستقل وربطه بتنبيهات deadlock/deadline/Outbox؛ أثبت تشغيل الإنتاج 93 والكشط التركيبي والعادي إغلاق P1-2.

الحكم التفصيلي والأدلة ومعايير القبول في [تدقيق اعتماد بيئة الإنتاج](PRODUCTION_ENVIRONMENT_AUDIT_AR.md). الحالة التشغيلية GO، واعتماد حوكمة البيئة NO-GO مؤقتًا حتى إغلاق P0-2.

## المرحلة 53 — تفعيل قياسات الإنتاج ومراقبتها

- [x] إضافة تمرير مستقل لـ`METRICS_BEARER_TOKEN` من سر بيئة `production` عبر `stdin` وملف مؤقت `0600`، دون وضعه في Artifact أو السجل.
- [x] دمج `METRICS_ENABLED=true` والرمز في بيئة CloudLinux الهدف مع الاحتفاظ بلقطة المصدر مستقلة للرجوع الذري وحذف الملفات المؤقتة في جميع حالات الخروج.
- [x] إضافة بوابة نشر تثبت `401` بلا رمز وكشطًا مصادقًا وتوقف النشر عند وجود تنبيه نشط.
- [x] إضافة مقياس آخر وقت إطلاق للتنبيه ومراقب خارجي كل ساعة يلتقط التنبيهات النشطة أو الحديثة، مع `workflow_dispatch` لتنبيه تركيبي لا يحقن عطلًا في الإنتاج.
- [x] إضافة اختبارات لعقد `Cache-Control: no-store`، ووصول سجل التنبيه، وحقن الرمز وحفظه عبر تبديل CloudLinux والرجوع دون إعادة تمريره.
- [x] إضافة السر إلى بيئة GitHub ودمج PR رقم 12؛ نجح تشغيل الإنتاج [93](https://github.com/ALMutasemObad/multi-company-accounting/actions/runs/32722561885)، وفشل تشغيل المراقبة [1](https://github.com/ALMutasemObad/multi-company-accounting/actions/runs/32723804308) عمدًا بتنبيه تركيبي موثق بعد كَشط ناجح، ثم نجح التشغيل العادي [2](https://github.com/ALMutasemObad/multi-company-accounting/actions/runs/32723895437).

## المرحلة 54 — أساس Inventory وإدارة المستودعات

- [x] إنشاء `Inventory` كـBounded Context مستقل و`Warehouse` كأول Aggregate مملوك له، مع وثيقة تحدد ما يدخل في المرحلة وما يبقى للأصناف والحركات والتكلفة لاحقًا.
- [x] إضافة نموذج `Warehouse` المعزول حسب الشركة والترحيل `20260824170000_inventory_warehouse_foundation`، مع تسلسل `WH-` ذري وصلاحيات `warehouses.view/manage` وإسنادها للأدوار الإدارية القائمة.
- [x] إضافة خمسة مسارات OpenAPI للعرض والبحث والإنشاء والتعديل والتعطيل، وحراس أجسام مولدة ونسخ متفائلة وسجل تدقيق داخل المعاملة.
- [x] إضافة شاشة مستودعات عربية وإنجليزية متجاوبة للبحث والترقيم والإنشاء والتعديل والتعطيل، مع رمز خادمي للقراءة فقط وربط أحداثها بسجل التدقيق.
- [x] إضافة اختبارات للعزل بين شركتين، وتفرد الرموز تحت الإنشاء المتزامن، وسباق تحديثين، ومنع الرمز اليدوي، والتعطيل المدقق، وحارس ملكية Inventory.
- [x] ترقية OpenAPI إلى `1.14.0`: 160 عملية و82 جسم طلب و1117 جسم استجابة مولدة؛ نجحت محليًا TypeScript والبناء والعقود والترجمة وعقد الواجهة و140 اختبار API غير المعتمد على قاعدة البيانات و20 اختبار واجهة و26 اختبار بنية تحتية. تختبر CI اختبارات قاعدة البيانات الـ60 والترحيل الكامل على MariaDB وMySQL.
- [x] لا تنشئ المرحلة أصنافًا أو أرصدة أو حركات أو تكلفة أو قيودًا محاسبية؛ هذه الحدود شرط قبول وليست نقصًا مخفيًا.

## المرحلة 55 — وحدات القياس وكتالوج الأصناف

- [x] إضافة `UnitOfMeasure` و`InventoryItem` مملوكين لـInventory ومعزولين حسب الشركة، بعلاقة مركبة تمنع ربط صنف بوحدة شركة أخرى حتى على مستوى قاعدة البيانات.
- [x] إضافة الترحيل `20260824200000_inventory_item_catalog`: جدولا الوحدات والأصناف، تسلسل أصناف `ITM-`، وصلاحيتا `inventory_catalog.view/manage` مع إسنادهما للأدوار الإدارية الحالية ومرجع تجهيز الشركات الجديدة.
- [x] اعتماد رمز وحدة قياس دلالي ثابت مثل `EA/KG` ودقة من صفر إلى ست منازل، ورمز صنف خادمي ثابت `ITM-000001` لا يقبله عقد الإدخال.
- [x] إضافة عشرة مسارات OpenAPI للعرض والبحث والتصفية والإنشاء والتعديل والتعطيل، وترقية العقد إلى `1.15.0`: 170 عملية و88 جسم طلب و1185 جسم استجابة مولدة.
- [x] منع تعطيل وحدة تستخدمها أصناف نشطة، وقفل صف الوحدة عند إنشاء/نقل الصنف وعند تعطيلها لمنع سباق ينتج صنفًا نشطًا على وحدة معطلة.
- [x] إضافة تبويبي وحدات القياس وكتالوج الأصناف بالعربية والإنجليزية، مع البحث والتصفية والترقيم والنماذج المتجاوبة والرموز الثابتة.
- [x] إضافة اختبارات تفرد الرموز تحت التزامن، وسباق تعديلين، وسباق إنشاء صنف مع تعطيل الوحدة، وعزل شركتين، ومنع الوحدة الأجنبية، والتدقيق، وحارس ملكية السياق والعقد.
- [x] نجحت محليًا 141/141 من اختبارات API غير المعتمدة على قاعدة البيانات، و20/20 للواجهة، و26/26 للبنية التحتية، والبناء وTypeScript والعقود والترجمة، و24/24 Playwright تغطي 216 حالة بصرية.
- [x] اجتياز اختبارات قاعدة البيانات الـ65 والترحيل الكامل على MariaDB 10.11 وMySQL 8.4، والفحص البصري وE2E والنسخ/الاستعادة وإنشاء الإصدار في تشغيل GitHub Actions رقم 116.
- [ ] دمج PR رقم 20 ونشره والتحقق العام من الإنتاج.

## المرحلة 56 — ربط تأليف الفواتير بكتالوج المخزون

- [x] إضافة مستودع اختياري إلى رأس فاتورة البيع والشراء، وصنف اختياري إلى كل بند، مع مفاتيح أجنبية مركبة بـ`companyId` ولقطات تاريخية لرمز واسم المستودع والصنف ورمز وحدة القياس.
- [x] إضافة الترحيل التوافقي `20260824230000_invoice_inventory_catalog_link`، وتوسيع دقة كمية بنود الفاتورة إلى `DECIMAL(19,6)` من دون تغيير السجلات القديمة أو جعل الروابط إلزامية عليها.
- [x] إنشاء `InventoryInvoiceCatalogPort` مملوك لـInventory؛ تستخدمه Sales وPurchases داخل المعاملة للتحقق من نشاط المرجع وعزله، واشتراط المستودع عند اختيار صنف، واحترام `decimalPlaces` للوحدة، من دون قراءة جداول Inventory مباشرة.
- [x] تحديث OpenAPI إلى `1.16.0` وواجهتي الفواتير لاختيار المستودع والصنف أو إبقاء بند يدوي، وملء الوصف تلقائيًا، وإظهار اللقطة التاريخية في التفاصيل وأرشيف/PDF فاتورة المشتريات.
- [x] إبقاء تحميل مرجعيات المخزون خاضعًا لصلاحيات `warehouses.view` و`inventory_catalog.view` من دون توسيع صلاحيات المبيعات؛ يظل مسار البند اليدوي يعمل إذا لم يملك المستخدم تلك الصلاحيات.
- [x] إضافة اختبارات للمنفذ والعقد والحدود المعمارية وربط فواتير البيع والشراء وثبات اللقطات ودقة الوحدة؛ نجحت محليًا 147/147 من اختبارات API غير المعتمدة على قاعدة البيانات و21/21 للواجهة و26/26 للبنية التحتية و48/48 Playwright، إضافة إلى TypeScript والبناء والعقود والترجمة وعقد الواجهة.
- [x] لم تنشئ المرحلة 56 حركة؛ أُنجز الربط الكمي لاحقًا في المرحلة 58، بينما بقي التقييم وقيد التكلفة خارج النطاق.

## المرحلة 57 — دفتر حركة المخزون الكمي والأرصدة

- [x] إضافة `InventoryMovementSequence` و`InventoryMovement` و`InventoryMovementLine` و`InventoryBalance` بعلاقات مركبة مع الشركة ولقطات تاريخية وقيد قاعدة يمنع الرصيد السالب.
- [x] تنفيذ الاستلام والصرف والتحويل والرصيد الافتتاحي وتسويتي الزيادة والنقص كحركات ترحّل فورًا ولا تملك مسارات تعديل أو حذف؛ يكون التصحيح بحركة جديدة.
- [x] تحديث الأرصدة والحركة والأسطر والتدقيق ونتيجة Idempotency داخل معاملة واحدة، مع تسلسل `IMV-` مستقل وترتيب أقفال حتمي للمستودعات والأصناف ومفاتيح الرصيد.
- [x] منع دقة تتجاوز وحدة القياس، وتكرار الصنف في الحركة، والرصيد الافتتاحي بعد وجود تاريخ، والصرف السالب، وتعطيل صنف أو مستودع يحمل كمية.
- [x] إضافة أربعة مسارات لعرض الأرصدة وقائمة الحركات وتفاصيلها وإنشاء الحركة، وصلاحيتي `inventory_movements.view/create`، وترقية OpenAPI إلى `1.17.0`: 174 عملية و89 جسم طلب و1210 أجسام استجابة مولدة.
- [x] إضافة تبويبي الأرصدة والحركات ونموذج متعدد الأسطر وتفاصيل اللقطات التاريخية بالقواميس الأربع، وتوسيع فحص الواجهة المتجاوبة ليشملهما.
- [x] إضافة اختبارات قواعد العمل والعقد والحدود، واختبارات قاعدة مسجلة للاستلام والصرف والتحويل والعزل وIdempotency وسباق صرفين ومنع التعطيل؛ نجحت محليًا اختبارات API غير المعتمدة على القاعدة 152/152 والواجهة 21/21 والبنية التحتية 26/26 وPlaywright ‏48/48، وتبقى اختبارات القاعدة الـ72 والترحيل 37 لبوابتي CI.
- [x] لم تربط المرحلة 57 الفاتورة؛ أُنجز الربط في المرحلة 58 من دون اختيار FIFO أو المتوسط أو إنشاء تكلفة مبيعات أو قيد تقييم.

## المرحلة 58 — ربط ترحيل الفواتير بدفتر المخزون

- [x] إنشاء `InventoryInvoiceStockPort` مملوك لـInventory؛ لا تكتب Sales أو Purchases في `InventoryMovement/InventoryBalance` مباشرة، ويعمل المنفذ داخل `Prisma.TransactionClient` نفسه الذي يملكه أمر الترحيل أو العكس.
- [x] اعتماد مصفوفة الاتجاه: فاتورة البيع `ISSUE`، الإشعار الدائن للمبيعات `RECEIPT`، فاتورة الشراء `RECEIPT`، والإشعار المدين للمشتريات `ISSUE`؛ ينشئ `REVERSE` الاتجاه المقابل كسجل immutable جديد.
- [x] تجاهل البنود اليدوية غير المخزنية، وتجميع تكرار الصنف في الفاتورة إلى سطر كمية واحد، وإعادة التحقق من المستودع والوحدة والصنف تحت أقفال Inventory قبل تغيير الرصيد.
- [x] إضافة `sourceType/sourceId/sourceEvent/sourceDocumentNumberSnapshot` وقيد تفرد على المصدر داخل الشركة وقيد `CHECK` all-or-none؛ تعيد إعادة المصدر المطابق نفس الحركة وترفض البيانات المختلفة.
- [x] تنفيذ الحركة في `beforeLedger` بعد قفل الفترة ومستند المصدر وعناصر AR/AP عند الصلة وقبل أقفال Ledger؛ نقص المخزون يعيد تغيير الذمة والقيد وحالة الفاتورة والحركة كلها.
- [x] إضافة اختبارات الاتجاهات الثمانية، وإعادة المصدر وتجميع البند، وعدم مضاعفة إعادة طلب الترحيل، وفشل بيع لنقص المخزون مع بقائه مسودة، وفشل عكس شراء مستهلك مع بقائه مرحلًا وذمته مفتوحة.
- [x] ترقية OpenAPI إلى `1.18.0` وإظهار مصدر الحركة في عقد القائمة والتفاصيل، وتحديث خريطة السياقات وسياسة الأقفال ووثيقة Inventory وREADME.
- [x] لا تنشئ المرحلة إشعارًا دائنًا أو مدينًا تلقائيًا، ولا تقييمًا أو COGS أو قيد مخزون؛ الفاتورة المرحلة هي مستند الكمية الانتقالي حتى اعتماد أذونات تسليم/استلام مستقلة.

## المرحلة 59 — بحث مرجعيات الفواتير المتدرج

- [x] إنشاء `ReferenceCombobox` مشترك يدعم البحث الخادمي المؤجل والتصفح صفحة بعد صفحة ودمج النتائج بلا تكرار وإلغاء الطلبات المتقادمة، مع لوحة مفاتيح ودلالات `combobox/listbox` ورسائل تحميل وفراغ وخطأ بالقواميس الأربع.
- [x] استبدال تحميل أول 100 سجل في محرري فواتير البيع والشراء بمحددات بحث للعملاء والموردين وفواتير المصدر والمستودعات والأصناف والحسابات والضرائب ومراكز التكلفة، مع الاحتفاظ بلقطات القيم القديمة عند تعديل المسودة.
- [x] قصر حسابات بند المبيعات على `REVENUE` القابلة للترحيل، وبند المشتريات على `ASSET/EXPENSE`، وحساب ضريبة المخرجات على `LIABILITY` والمدخلات على `ASSET`؛ تنفذ المرشحات في API داخل نطاق الشركة.
- [x] إضافة البحث والتصفح إلى نسب ضريبة المبيعات والمشتريات وإدارة صفحاتهما، وتطبيق مرشح `active` لمراكز التكلفة، واستخدام المحدد المتدرج في تقريري أعمار العملاء والموردين.
- [x] ترقية OpenAPI إلى `1.19.0` لتوثيق `allowsPosting/accountClasses` وتصفح الضرائب و`PaginationMeta`، وإعادة توليد الحراس إلى 89 جسم طلب و1210 جسم استجابة عبر 174 عملية.
- [x] إضافة اختبارات وحدة لعزل ودمج مرشحات الحسابات ومراكز التكلفة والضرائب، ولمسارات البحث ودمج الصفحات في الواجهة، وحارس يمنع رجوع حد 100؛ نجحت 157/157 من اختبارات API غير المعتمدة على القاعدة و23/23 للواجهة و16/16 من حالات Playwright المستهدفة عبر اللغات والأحجام الأربعة.
- [x] لا تضيف المرحلة Migration ولا تغير قواعد إنشاء الإشعار الدائن/المدين أو تقييم المخزون وCOGS؛ تعالج قابلية التوسع واختيار المرجع الصحيح فقط.

## المرحلة 60 — النسخ الإنتاجي الخارجي وتمرين التعافي

- [x] إضافة `create-cpanel-offsite-backup.sh` بمسارات صريحة و`umask 077` وقفل `.pipeline.lock` البعيد؛ يقرأ الأسرار عبر `stdin`، يثبت تطابق هدف حسابي Passenger والترحيل قبل النسخ، ويتحقق من الملفات والصلاحية والحجم وSHA-256 والترحيلات قبل طباعة نتيجة منقحة.
- [x] ترقية النسخ الجديدة إلى Manifest ‏`mcap-backup-v2` واسم `mcap-production-*` عامين؛ حُذفت منهما هوية قاعدة المصدر وإصدارها وأعداد الصفوف والمجاميع المالية، مع إبقاء استعادة `mcap-backup-v1` القديمة متوافقة.
- [x] إضافة `verify-backup-artifact.mjs` ومكتبة قابلة للاختبار ترفضان التلاعب وتبديل الاسم والتقادم والتوقيت المستقبلي وصيغة الحماية غير المعتمدة، وترفضان ظهور حقول قاعدة البيانات المحمية في Manifest الجديد.
- [x] إضافة Workflow يومي مقيد صراحة بـ`refs/heads/main` ورأسه الحالي ينقل `.jwb` وManifest إلى GitHub Actions Artifact خارج حساب iFastNet باحتفاظ 90 يومًا وضغط صفري؛ ثُبتت Actions بالـcommit SHA وصور MariaDB/MySQL بالـdigest في مساري التعافي وCI، وحُصر مفتاح التشفير في خطوتي إنشاء النسخة والاستعادة فقط، ولا يصل داخل Job الاستعادة إلا إلى خطوة فك التشفير نفسها.
- [x] توحيد ملف القفل البعيد `.pipeline.lock` بين النسخ والنشر مع انتظار محدود 900 ثانية لمنع تزامن `mysqldump` مع أي Prisma Migration أو تفعيل إنتاجي، وإزالة الاعتماد على طابور GitHub `concurrency` المشترك الذي قد يستبدل الطلب المنتظر الأقدم.
- [x] إضافة احتفاظ محلي محدود بسبعة أيام لنمط `mcap-production-*` بعد نجاح الرفع الخارجي فقط وتنظيف مخلفاته اليتيمة بعد يوم؛ لا يمس التنظيف نسخ ما قبل النشر، ويفشل بأمان عند اختلاف المسار أو وجود رابط رمزي.
- [x] تسجيل `snapshotStartedAt/recoveryPointAt` وقياس RPO بحد 26 ساعة مع بوابة Artifact محافظة 25.5 ساعة؛ يثق المراقب فقط في Artifacts الصادرة من Workflow التعافي على `main`.
- [x] إضافة تمرين في أول يوم من كل شهر أو يدويًا يعيد Artifact المنشأ في التشغيل نفسه إلى MariaDB 10.11.11 مؤقتة وفارغة، ويعيد التحقق من التشفير والجداول والترحيلات وقابلية استعلام التعدادات ووجود بيانات الهوية الأساسية واتزان المدين والدائن، ويقيس زمن تنفيذ الاستعادة التقني بحد 900 ثانية ويحفظ `dr-evidence.json` منقحًا؛ لا يدعي v2 مقارنة أعداد المصدر التي حُذفت منه للخصوصية.
- [x] مراقبة عمر آخر تمرين ناجح بحد 35 يومًا وحفظ علامة فشل دائمة لأي تمرين مطلوب فاشل؛ يبقى المراقب أحمر حتى يوجد نجاح أحدث، مع توثيق أن توقف منصة GitHub Actions نفسها يحتاج Heartbeat خارجيًا.
- [x] توثيق الاستجابة للفشل وفقدان الاستضافة وتدوير المفتاح وحدود عدم توفر binlogs، وإضافة ثمانية اختبارات بنية للتحقق من Artifact والتنقيح الذري ومسار الأسرار والجدولة والاستعادة والتنبيه؛ لا تضيف المرحلة Migration أو تغير التطبيق أو قاعدة الإنتاج.
- [x] دُمجت المرحلة ونُشرت في تشغيل [32839845689](https://github.com/ALMutasemObad/multi-company-accounting/actions/runs/32839845689) على الالتزام `c39b97949655f43c9fe35f98b3d47cce5c6c6054` ببصمة حزمة `sha256:8a4946ed614f536e3bdd230f1b93a853d0b2677a9dea645b43951e795408f5ef`، ونجح قبول `restore_drill=true` في تشغيل [32840949844](https://github.com/ALMutasemObad/multi-company-accounting/actions/runs/32840949844): حُفظ Artifact النسخة 90 يومًا ببصمة `sha256:63ad16b7cb127d35de2f92feb872894ed2decf611c1f12f73778f127ae84c5a9`، وطابقت بصمة الملف المشفر `543e52829ac0727b922180948e4151afd4e949d51035df850c907c40ea215820` الـManifest المنقح، وكان عمر نقطة الاستعادة 7 ثوانٍ عند التحقق. أعادت MariaDB 10.11.11 المعزولة 56 جدولًا و38 Migration خلال ثانية واحدة مقابل هدف 900 ثانية، وحُفظ دليل `accepted_bootstrap` ببصمة `sha256:928b365e7a5f35a4fbc966b53788c5ebfc6427d4f8022d51a928b8785b7ffbdf` لأن هذا أول Artifact موثوق؛ ثم نجح تشغيل المراقبة [32841244870](https://github.com/ALMutasemObad/multi-company-accounting/actions/runs/32841244870) دون تنبيه.

## المرحلة 61 — كشف الحساب وتدقيق أثر الفواتير على المخزون

- [x] تحويل كشف الأستاذ الخلفي إلى تقرير مستقل ظاهر في واجهة التقارير للعميل أو المورد أو حساب الأستاذ، مع اختيار مرجعي قابل للبحث والتصفح لا يستبعد الأطراف المعطلة تاريخيًا، وفترة لا تتجاوز 366 يومًا، وترقيم صفحات.
- [x] إظهار الرصيد الافتتاحي والحركات المرحّلة والرصيد الجاري والختامي بعملة الشركة، وإكمال عقد الاستجابة بالشركة والعملة وصاحب الحساب وحقول المستند الفعلية.
- [x] إضافة تصدير CSV وExcel وPDF عربي حتى 10000 حركة، مع تنقية CSV من الصيغ، وترويسات العدد والاقتطاع، وسجل تدقيق، وصلاحية `reports.ledger.export` مستقلة يضيفها الترحيل 39 للمدير.
- [x] ترقية OpenAPI إلى `1.20.0` وإعادة توليد العقد إلى 175 عملية و89 جسم طلب و1215 جسم استجابة، مع اختبارات مسار التصدير والجدول العربي والاستعلام الذي يرسل صاحب حساب واحدًا فقط.
- [x] إعادة تدقيق تكامل الفواتير بالمخزون: الشراء وإشعار المبيعات الدائن يزيدان الكمية، والبيع وإشعار المشتريات المدين ينقصانها، والعكس يقلب الاتجاه داخل معاملة `PostingEngine` نفسها؛ يبقى تقييم التكلفة وCOGS وأذونات الاستلام والتسليم المستقلة خارج النطاق الحالي ومذكورًا كعمل متبقٍ.
- [x] نجحت اختبارات API غير المعتمدة على القاعدة 159/159 واختبارات الواجهة 24/24 وTypeScript والبناء وPrisma validation والعقد والترجمة، ونجح فحص متصفح فعلي عربي مكتبي وهاتفي لكشف عميل ذي حركات وتصديراته دون تمدد صفحة أو خطأ ظاهر.
- [ ] إعادة بوابة قاعدة البيانات الـ72 والترحيل 39 على MariaDB/MySQL قبل الإصدار؛ تعذر تشغيلها محليًا لأن MariaDB في XAMPP رفض البدء بخطأ `Aria recovery failed` قبل تنفيذ أي اختبار، ولم تُحذف سجلات Aria أو تُمس بيانات المستخدم.

## المرحلة 62 — تقييم المخزون وتكلفة المبيعات

- [x] تحويل دفتر Inventory من كمي فقط إلى كمي وقيمي وفق المتوسط المرجح المتحرك، مع تكلفة وحدة وإجمالي تاريخيين على سطر الحركة، وقيمة ومتوسط تكلفة وحالة تهيئة على رصيد المستودع/الصنف.
- [x] إلزام الحركات الداخلة اليدوية بتكلفة العملة الأساسية، وحمل تكلفة التحويل من المصدر إلى الوجهة، واستهلاك المتوسط في الصرف، ومنع القيمة السالبة أو بقاء قيمة بلا كمية.
- [x] وسم الأرصدة الموجبة السابقة بأنها غير مقيّمة ومنع حركتها حتى تهيئة أحادية مدققة ومحمية بـIdempotency و`version`؛ أضيفت واجهة تعرض القيمة والمتوسط وحالة التقييم ونموذج التهيئة.
- [x] تقييم فاتورة الشراء بصافي البند الأساسي، وإضافة COGS/Inventory إلى قيد البيع، واستعادة التكلفة التاريخية في إشعار المبيعات الدائن والعكس، ومعالجة فرق سعر إشعار المشتريات المدين عن المتوسط في حساب تكلفة البضاعة.
- [x] إبقاء Inventory خارج ملكية Ledger: يعيد `InventoryInvoiceStockPort` حقائق التقييم فقط، وتبني Sales/Purchases الأسطر التي ينشئها `PostingEngine` داخل المعاملة نفسها.
- [x] منع مجموع كميات إشعارات الصنف المسودة والمرحلة من تجاوز كمية الصنف في فاتورة البيع أو الشراء الأصلية، مستقلًا عن حد المبلغ المالي.
- [x] إعادة فحص حد كمية المرتجع بعد قفل الفاتورة الأصلية لمقاومة التزامن، وتخصيص آخر جزء من إشعارات المبيعات الدائنة المجزأة من باقي التكلفة التاريخية لمنع فرق التقريب.
- [x] توسيع OpenAPI والواجهة والترجمات والتدقيق واختبارات المعمارية والتكامل لتغطية القيمة والمتوسط وCOGS وفرق المرتجع والتكلفة التاريخية.
- [ ] لم يُنشر هذا العمل ولم يُرفع إلى أي مستودع بعيد بناءً على توجيه المستخدم؛ يلزم تشغيل بوابات MariaDB/MySQL الكاملة قبل أي طلب نشر لاحق.

## المرحلة 63 — إغلاق طباعة المبيعات واستيراد أصناف الفواتير

- [x] إضافة لقطة تاريخية مؤرشفة عند ترحيل فاتورة المبيعات أو إشعارها الدائن، ومسار PDF عربي يعيد النسخة نفسها ببصمة SHA-256 وعدّاد طباعة وتدقيق وصلاحية `sales_invoices.print` مستقلة.
- [x] تعميم نموذج طباعة الفاتورة على العميل أو المورد مع الحفاظ على قراءة لقطات المشتريات السابقة، وإظهار زر الطباعة للمستند المرحّل أو المعكوس في واجهة المبيعات.
- [x] توسيع قوالب CSV/XLSX للفواتير بحقلَي `warehouse_code` و`inventory_item_code` الاختياريين، وحل المرجعيات من خلال `InventoryInvoiceCatalogPort` لا بقراءة جداول Inventory من سياق الفاتورة.
- [x] حفظ المستودع ولقطته والصنف ولقطته في المسودة المستوردة، وإخضاعها لمسار `prepare` نفسه، مع قبول ملفات القالب السابقة التي لا تحمل العمودين الجديدين.
- [x] ترقية تغطية OpenAPI إلى 177 عملية و90 جسم طلب و1227 جسم استجابة، وإضافة الترحيل 41 لصلاحية طباعة المبيعات.
- [ ] لم يُنشر هذا العمل ولم يُرفع إلى أي مستودع بعيد؛ تبقى بوابات قاعدة البيانات الفعلية مطلوبة قبل أي طلب نشر لاحق.

## المرحلة 64 — إغلاق محاسبة الحركات اليدوية وعكسها

- [x] إغلاق فجوة كانت تسمح للحركة اليدوية المقيّمة بتغيير قيمة `InventoryBalance` من دون أثر في الأستاذ العام: كل `OPENING_BALANCE/RECEIPT/ISSUE/ADJUSTMENT_IN/ADJUSTMENT_OUT` جديد ينشئ مستند `INVENTORY_ADJUSTMENT` ويُرحّل قيده عبر `PostingEngine` داخل معاملة الحركة نفسها؛ يبقى `TRANSFER` بلا قيد لأنه لا يغيّر إجمالي قيمة المخزون.
- [x] تثبيت سياسة افتراضية لا تتطلب خبرة محاسبية من المستخدم: الرصيد الافتتاحي يقابل `retained-earnings`، والزيادة اليدوية تقابل `misc-income`، والنقص اليدوي يقابل `misc-expense`، مع التحقق من حساب المخزون والحساب المقابل وصنفهما وحالتهما وقابليتهما للترحيل.
- [x] إضافة حالة ونسخة وروابط محاسبية وعكسية للحركة، ومسار `POST /inventory-movements/{movementId}/reverse` وصلاحية `inventory_movements.reverse`: ينشئ العكس حركة مقابلة بالتكلفة التاريخية ويعكس القيد نفسه، ويحدّث الأصل إلى `REVERSED` ذريًا، ويرفض عكس حركة مولدة من فاتورة أو عكس حركة العكس.
- [x] ربط حركة `REVERSE` التي تنشئها الفاتورة بحركة `POST` الأصلية وتحويل حالة الأصل إلى `REVERSED` داخل معاملة الفاتورة؛ يمنع `status/version` والتفرد عكسًا ثانيًا، مع الحفاظ على سياسة عكس الحركة المولدة من مستندها المصدر فقط.
- [x] تضمين ترقية رجعية حتمية في الترحيل 42 تربط حركات `REVERSE` التي أنشأتها الفواتير قبل التحديث بحركات `POST` الأصلية، وتصحح حالة ونسخة الأصل بدل ترك السجل التاريخي بحالة `POSTED` مضللة.
- [x] مراعاة الحركات التاريخية السابقة للترحيل 42: يبقى رابط المستند اختياريًا لها، ويكون عكسها stock-only لأنها لم تنشئ قيدًا أصلًا؛ أما الحركة اليدوية الجديدة ذات القيمة الصفرية فترفض بدل صنع مستند محاسبي فارغ.
- [x] إكمال واجهة تفاصيل الحركة بحالتها ومستندها وحسابها المقابل وروابط العكس، وإضافة نافذة عكس بالتاريخ والسبب، وتحديث الترجمات الأربع ورسائل الأعمال والتدقيق.
- [x] إغلاق فجوة مرشح تقرير اليومية الذي كان عقده يقبل أنواعًا لا يقبلها Router: أصبح المرشح والواجهة يعرضان كل أنواع البيع والشراء والإشعارات وتسوية المخزون والإقفال والقبض والصرف والقيد اليدوي.
- [x] ترقية OpenAPI إلى `1.22.0` و178 عملية و91 جسم طلب و1237 جسم استجابة، وإضافة الترحيل 42 لنوع المستند والروابط والحالة والنسخة وصلاحية العكس.
- [x] نجحت اختبارات API غير المعتمدة على القاعدة 162/162 واختبارات الواجهة 24/24 والبنية التحتية 34/34، ونجحت TypeScript لكل التطبيق والاختبارات وبناء API وVite وPrisma validation وتوليد العميل والعقد والترجمة وعقد الواجهة.
- [ ] لم تشغّل اختبارات قاعدة البيانات الـ73 أو يطبق الترحيل 42 محليًا لأن MariaDB في XAMPP ما زال يتوقف قبل الاتصال بخطأ `Aria recovery failed`؛ يجب إعادة بوابتي MariaDB 10.11 وMySQL 8.4 قبل أي إصدار.
- [ ] لم يُنشر هذا العمل ولم يُرفع إلى أي مستودع بعيد تنفيذًا لتوجيه المستخدم الصريح.

## المرحلة 65 — إغلاق القيمة الدفترية وفروقات العملة عند التسوية

- [x] إضافة `originalBaseAmount/outstandingBaseAmount` إلى عناصر الذمم المدينة والدائنة؛ يحمل كل عنصر الآن الرصيد بالعملة الأصلية وقيمته الدفترية الدقيقة بعملة الأساس، وتفرض القيود تطابق حالة الرصيدين.
- [x] جعل تسوية القبض والدفع تستهلك القيمة الدفترية تحت قفل عنصر الذمة: الجزئي يأخذ نسبة مقربة إلى أربع منازل، والنهائي يأخذ الباقي كاملًا، ويعيد المنفذ حقيقة القيمة المسحوبة إلى Treasury داخل المعاملة نفسها.
- [x] حفظ `carryingBaseAmount/settlementBaseAmount/realizedFxBaseAmount` في كل تخصيص عند الترحيل، وتركها `null` في المسودة، واستخدام اللقطة نفسها عند العكس حتى لا تتأثر النتيجة بسعر صرف أو بيانات رئيسية لاحقة.
- [x] تسجيل الفرق تلقائيًا في القيد: في القبض الفرق = قيمة التحصيل − قيمة الذمة، وفي الدفع = قيمة الذمة − قيمة الدفع؛ الموجب ربح والسالب خسارة. أضيف حسابا الدليل الافتراضي 4220 و5520، ولا يطلب النظام من المستخدم قرارًا محاسبيًا.
- [x] توسيع `PostingEngine.beforeLedger` ليعيد خطة قيد نهائية اختيارية بعد تثبيت حقائق المجالات المقفلة، مع بقاء الخطط القديمة المتحولة داخل hook متوافقة؛ يظل المحرك الكاتب الوحيد للـLedger.
- [x] إلزام الإشعار الدائن/المدين بسعر صرف فاتورة المصدر وتخفيض رصيد الأساس بقاعدة الإشعار الفعلية، ومنع إغلاق مبلغ العملة الأصلية إذا لم يغلق معه الرصيد الدفتري.
- [x] إضافة الترحيل 43 بترقية رجعية حتمية للأرصدة ولقطات التخصيص وتوزيع فرق التقريب على آخر سطر. يفشل الترحيل عمدًا عند وجود تسوية فعالة قديمة بسعر مخالف لم يسجل إصدارها فرق عملة، أو إشعار فعال بسعر مخالف، أو رصيد تاريخي غير قابل للمطابقة؛ لا يخترع قيودًا داخل فترات قد تكون مغلقة.
- [x] ترقية OpenAPI إلى `1.23.0` وإظهار الرصيد الدفتري وفروق العملة الموقعة، وتحديث واجهتي تفاصيل القبض والدفع والترجمات الأربع ورسائل الأعمال والتدقيق والوثائق المعمارية.
- [x] نجحت محليًا حزمة API الكاملة 239/239 في 61 ملفًا على MariaDB 10.4.32، واختبارات الواجهة 24/24، والبنية التحتية 34/34، وTypeScript للخادم والاختبارات والواجهة، وتوليد Prisma والعقد وبوابة الترجمات.
- [x] طبقت سلسلة الترحيلات الـ43 من قاعدة فارغة ونجح الـSeed على MariaDB 10.4.32 مؤقتة ومعزولة، بعد إزالة قيد ذاتي غير متوافق في الترحيل 42؛ ونجحت اختبارات القبض والصرف المستهدفة 10/10، ومنها ترحيل تحصيل أجنبي 100 بقيمة دفترية 130 وتسوية 125 وخسارة 5 ثم عكسه واستعادة الرصيدين بدقة.
- [x] أغلقت الفجوات التي كشفتها الحزمة الشاملة: يطبق Seed دليل الحسابات الافتراضي، وتجهز اختبارات الحركات فترة مفتوحة، ويُفحص نقص كمية المخزون قبل نقص قيمته، وصُحح استعلام تكاليف مرتجعات المبيعات وربط خدمة طباعتها، وأصبحت اختبارات الأمان مستقلة، وينتظر تكرار رمز التسجيل المتزامن نتيجة التجهيز حتى عشر ثوانٍ. نجحت بعدها 239/239 من قاعدة فارغة.

## المرحلة 66 — إغلاق سند الطرف غير الموزع

- [x] منع إنشاء أو تعديل أو ترحيل سند قبض مرتبط بعميل أو سند صرف مرتبط بمورد من دون توزيع كامل يساوي مبلغ السند؛ وبذلك لا ينشأ أثر AR/AP في الأستاذ من دون أثر مطابق في عنصر الذمة.
- [x] إبقاء سند الحساب المقابل المباشر صالحًا من دون توزيعات، وإخفاء محرر التوزيعات عند اختياره ومسح أي توزيعات طرف قديمة من النموذج.
- [x] إضافة سبب العمل `ALLOCATION_REQUIRED` ورسائله باللغات الأربع، وتوضيح القاعدة في OpenAPI وعقد تسوية AR/AP، من دون Migration أو جدول أو مسار HTTP أو وحدة وظيفية جديدة.
- [x] نجحت اختبارات القبض والصرف المستهدفة 10/10، وحزمة API الكاملة 239/239 على MariaDB 10.4.32 من قاعدة فارغة بعد 43 ترحيلًا والـSeed، واختبارات الواجهة 25/25 والبنية التحتية 34/34 وTypeScript وبناء API وVite والعقد والترجمة وعقد الواجهة.
- [ ] ما زال مسار الترقية التاريخية وبوابتا MariaDB 10.11 وMySQL 8.4 مطلوبين قبل الإصدار؛ النجاح المحلي على MariaDB 10.4 لا يجيز النشر، ولم يُنشر هذا العمل.
- [ ] لم يُنشر هذا العمل ولم يُرفع أو يُدمج في أي مستودع بعيد تنفيذًا لتوجيه المستخدم الصريح.

## المرحلة 67 — Backend المطابقة البنكية 1:1

- [x] تحويل Spike القارئ إلى شريحة خلفية مغلقة داخل `Treasury`: Preview بلا حفظ، واعتماد وارد CSV/CAMT.053 ببصمة وIdempotency، وقوائم وتفاصيل، وجلسة لنطاق حساب بنك واحد.
- [x] إضافة `BankStatementImport/Line` و`BankReconciliationSession/Match` بعلاقات مركبة مقيدة بالشركة، وتفرد للوارد والجلسة والسطر والـMatch المعتمد، مع حفظ تاريخ الفك بدل الحذف الصلب.
- [x] إنشاء `ReconciliationLedgerQueryPort` للقراءة من القيود `POSTED` فقط و`ReconciliationMatcherPort` بمحرك pure حتمي: مرجع+مبلغ+عملة ثم مبلغ+عملة+نافذة تاريخ، بلا tolerance أو fuzzy أو FX أو اعتماد تلقائي.
- [x] إضافة 14 عملية OpenAPI للمعاينة والاستيراد والجلسات والحركات والاقتراح والاعتماد والربط اليدوي والفك والتصنيف والإقفال، وترقية العقد إلى `1.24.0` و192 عملية و100 جسم طلب و1317 جسم استجابة، مع حراس الطلب والاستجابة وتطابق Router.
- [x] إضافة صلاحيات `view/import/review/close` ومنحها لدور `ADMINISTRATOR` فقط؛ يثبت الاختبار أن دورًا مخصصًا يمكنه المراجعة دون الإقفال. كل أمر كتابة محمي بـCSRF وIdempotency وAudit ونسخة الجلسة.
- [x] تطبيق ترتيب القفل: الجلسة ثم خطوط البنك تصاعديًا ثم حركات Ledger تصاعديًا، مع deadline وretry المحدود وقيود تفرد تمنع اعتماد السطر أو الحركة مرتين تحت التزامن.
- [x] إثبات عدم كتابة Ledger: الـParser بلا Prisma، والـMatcher pure، والـAdapter Query-only، واختبار التكامل يثبت ثبات عددي `JournalEntry/JournalLine` طوال الاستيراد والاقتراح والاعتماد والفك والربط والإقفال. أي حركة مفقودة تبقى أمرًا في Receipt/Payment/Manual Journal وتمر عبر `PostingEngine`.
- [x] إضافة الترحيل 44 وتطبيق جميع الترحيلات من قاعدة فارغة مع `migrate status` والـSeed المرجعي والتجريبي الاصطناعي، ثم نجاح حزمة API `263/263` في 64 ملفًا على MariaDB 10.4.32 المعزولة. حذفت قاعدة `jiwar_finance_reconciliation_gate` وحدها بعد التحقق من اسمها.
- [x] إبقاء `BANK_RECONCILIATION_ENABLED=false` افتراضيًا؛ لم تضف واجهة أو رابطًا ظاهرًا أو نشرًا، ولم يحدث push. تبقى مصفوفة CI على MariaDB 10.11 وMySQL 8.4 وبوابات الإصدار إلزامية قبل أي إطلاق.

## المرحلة 68 — واجهة المطابقة البنكية والإطلاق التدريجي

- [x] إضافة تبويب **التسوية البنكية** داخل الخزينة، ويظهر فقط إذا سمحت البيئة والشركة وصلاحية المستخدم؛ لا يترك التعطيل رابطًا ميتًا أو يغير سلوك صفحة الخزينة السابقة.
- [x] تنفيذ رحلة كشف جديد: اختيار حساب وصيغة CSV/CAMT.053، ضبط أعمدة CSV، قراءة محلية ضمن حد 512KB، المعاينة، اعتماد الاستيراد، ثم إنشاء جلسة للوارد كاملًا.
- [x] تنفيذ مساحة المراجعة ذات الجدولين المتقابلين لحركات البنك والدفاتر، مع أسباب الاقتراح ومرشحات الحالة والمبلغ والتاريخ وإجراءات الاقتراح والاعتماد والربط اليدوي والفك والتصنيف.
- [x] عرض أرصدة البنك والدفاتر والفرق، ومنع الإقفال عند وجود أسطر غير محلولة أو فرق غير صفري، مع بقاء الخدمة هي الحارس النهائي للتزامن والسياسة.
- [x] إضافة تصدير CSV محلي لتقرير المطابقة والاستثناءات مع حماية خلايا الصيغ واستبعاد الملف الخام والبصمات والمعرفات الداخلية.
- [x] إضافة Policy للإطلاق `OFF/SHADOW/REVIEW/CLOSE` وقائمة سماح للشركات وEndpoint قدرات يدمج المرحلة مع RBAC، وحراسة كل مسار في الخادم. يمنع wildcard لقائمة الشركات في الإنتاج.
- [x] ترقية OpenAPI إلى `1.25.0` و193 عملية و100 جسم طلب و1320 جسم استجابة، وتحديث الحراس المولدة واختبارات أعداد العقد.
- [x] إكمال مفاتيح العربية والإنجليزية والأردية والهندية، ونجاح عقد الترجمة والواجهة، واختبارات المنطق `28/28`، واختبارات API غير المعتمدة على القاعدة `191/191`، واختبارات البنية `34/34`.
- [x] نجاح 64 حالة Responsive للغات الأربع عند 390 و768 و1440 و1920 بكسل، ورحلة Chromium كاملة من المعاينة إلى إقفال جلسة بفارق صفر، وبناء API وVite وTypeScript.
- [x] التفعيل محليًا لشركة **شركة جوار** في مرحلة `CLOSE` للمعاينة اليدوية. لم يحدث نشر أو Push أو تعديل لأي بيئة بعيدة، ولا تضيف هذه المرحلة موديولًا جديدًا أو ترحيل قاعدة بيانات.

## المرحلة 69 — الإقفال المالي الشهري والسنوي المراجع

- [x] استبدال الإقفال المباشر بمساحة عمل كاملة داخل صفحة السنوات والفترات: فحص الجاهزية، بدء دورة تحضير، تحديث الفحص، اعتماد المراجعة، الإعادة بسبب موثق، ثم الإقفال النهائي. لا يصل زر الإقفال إلى الخادم من دون تشغيل `REVIEWED` ونسخته.
- [x] إضافة آلة انتقال pure للحالات `OPEN → PREPARING → REVIEWED → CLOSED` ومسار الإعادة وإعادة الفتح باستخدام `xstate@5.32.5` exact خلف Adapter واحد، مع رخصة MIT وNOTICE وبصمة الحزمة واختبار يمنع انتشار الاستيراد خارج حد Fiscal.
- [x] إضافة ثمانية فحوص جاهزية: ترتيب الفترات، المسودات، اتزان الأستاذ، تطابق الذمم، اكتمال المطابقة البنكية، جاهزية المخزون، أسعار إقفال العملات المستخدمة، وحساب الأرباح المبقاة `3300` في نهاية السنة.
- [x] حفظ لقطة قائمة الجاهزية وحزمة الإقفال وبصمتي `SHA-256`، وإعادة حسابهما داخل معاملة الإقفال. أي حركة Ledger بعد المراجعة ترفض الإقفال بـ`CHECKLIST_CHANGED` وتفرض مراجعة جديدة.
- [x] إقفال آخر فترة يصفر حسابات الإيرادات والمصروفات ويرحل صافي النتيجة إلى `3300` في مستند `PERIOD_CLOSE` عبر `PostingEngine`. لا ينشئ قيد افتتاح مكرر، وتعكس إعادة الفتح مستند النتيجة ذريًا قبل إكمال العملية.
- [x] إضافة الترحيل 45 وجدول `financial_close_runs` بعزل شركة وعلاقات مركبة وقيود سلامة ونسخ متفائلة وIdempotency وAudit وترتيب قفل الفترة ثم التشغيل.
- [x] ترقية OpenAPI إلى `1.26.0` وإضافة 6 عمليات لدورة الإقفال، ليصبح العقد 199 عملية و104 أجسام طلب و1358 مخطط استجابة، مع الحراس المولدة وتطابق المسارات.
- [x] إكمال الواجهة والترجمات الأربع وحالات القائمة والحزمة والإعادة والإقفال السنوي، ونجاح 16 حالة بصرية جديدة عبر اللغات الأربع وعروض 390/768/1440/1920، لترتفع المصفوفة إلى 80 حالة.
- [x] تطبيق جميع الترحيلات الـ45 والـSeed من قاعدة فارغة على MariaDB 10.4.32 معزولة، ثم نجاح حزمة API `273/273` في 67 ملفًا؛ شمل ذلك العزل والتزامن وIdempotency وتغير القائمة بعد المراجعة وقيد نتيجة سنة بمبلغ 150 وعكسه، ثم حذفت قاعدة الاختبار المحددة وحدها.
- [x] نجاح TypeScript للخادم والاختبارات والواجهة، وPrisma validate/generate، وبناء API وVite، والعقد والترجمة وعقد الواجهة، واختبارات الواجهة `28/28` والبنية `34/34`. لم يحدث نشر أو Push.
- [ ] إعادة التقييم الآلي لفروق العملة غير المحققة ليست منفذة عمدًا قبل اعتماد تصنيف الحسابات النقدية وحسابي الربح والخسارة وسياسة العكس؛ يمنع النظام حاليًا الإقفال عند غياب أسعار الإقفال اللازمة.

## ترحيلات قاعدة البيانات

1. `20260801171000_initial_identity`
2. `20260801173000_rbac`
3. `20260801180000_users_audit`
4. `20260801183000_fiscal`
5. `20260801190000_accounting_document_header`
6. `20260801193000_journal_reconciliation`
7. `20260801200000_accounts_cost_centers`
8. `20260801210000_manual_journal_controls`
9. `20260801220000_receipts_reference_data`
10. `20260801230000_suppliers_payments`
11. `20260809140000_reports_permission`
12. `20260809193000_company_admin_permissions`
13. `20260811100000_document_print_archives`
14. `20260811130000_audit_log_access`
15. `20260811220000_financial_statements`
16. `20260813120000_journal_report`
17. `20260813190000_sales_invoices`
18. `20260813210000_purchase_invoices`
19. `20260813230000_purchase_invoice_printing`
20. `20260813233000_security_events`
21. `20260820180000_multi_company_provisioning`
22. `20260820223000_mysql84_session_constraints`
23. `20260821150000_company_currencies_exchange_rates`
24. `20260821170000_default_chart_template`
25. `20260821200000_company_owned_currencies`
26. `20260821220000_self_registration`
27. `20260822100000_transactional_outbox`
28. `20260822150000_auto_master_data_codes`
29. `20260822210000_expand_auto_codes_and_password_reset`
30. `20260822230000_receivable_payable_items`
31. `20260822233000_tax_context_ownership`
32. `20260822234500_treasury_context_ownership`
33. `20260822235900_data_import_center`
34. `20260824170000_inventory_warehouse_foundation`
35. `20260824200000_inventory_item_catalog`
36. `20260824230000_invoice_inventory_catalog_link`
37. `20260824233000_inventory_movement_ledger`
38. `20260825003000_invoice_inventory_movements`
39. `20260825120000_account_statement_export`
40. `20260826190000_inventory_weighted_average_costing`
41. `20260826200000_sales_invoice_printing`
42. `20260826213000_manual_inventory_accounting_reversal`
43. `20260826230000_realized_fx_settlements`
44. `20260827010000_bank_reconciliation_backend`
45. `20260827040000_financial_close_workflow`

أثبت تشغيل GitHub Actions رقم 114 تطبيق الترحيلات الـ34 السابقة وترقية المستودعات على MariaDB 10.11.11 وMySQL 8.4.11 ثم نشرها، وأثبت تشغيل PR رقم 116 تطبيق الترحيل 35 وجدولي وحدات القياس والأصناف وتسلسله وصلاحياته من قاعدة فارغة. وأثبت تشغيل الإنتاج [32825710229](https://github.com/ALMutasemObad/multi-company-accounting/actions/runs/32825710229) تطبيق الترحيلات 36 و37 و38 من قاعدة فارغة وعلى مسار الترقية في المحركين، ثم نشر ربط الفواتير ودفتر الحركة والبحث المتدرج والتحقق من الإنتاج على الالتزام `eaf56a7`.

## نتائج التحقق الحالية

- [x] Prisma schema validation.
- [x] Prisma Client generation.
- [x] TypeScript لجميع workspaces والاختبارات.
- [x] 417 تحققًا آليًا: 273 للـAPI، و28 للواجهة، و34 للبنية التحتية، و80 للمصفوفة المتجاوبة، ورحلتا E2E؛ ويضاف إليها سيناريو نجاح الرجوع وسيناريو فشل الهدف والاستعادة الآمنة في سكربت CI.
- [x] نجحت محليًا حزمة API الكاملة 273/273 على MariaDB 10.4.32 من قاعدة فارغة بعد 45 ترحيلًا والـSeed، واختبارات الواجهة 28/28 والبنية التحتية 34/34 والمصفوفة المرئية 80/80 عبر اللغات الأربع وعروض 390/768/1440/1920؛ نجحت كذلك TypeScript وتوليد Prisma والعقد والترجمة، وتبقى بوابتا MariaDB 10.11/MySQL 8.4 قبل النشر.
- [x] اختبار تجهيز فعلي ينشئ شركتين مستقلتين لهوية مستخدم عالمية واحدة ويثبت عدم مضاعفة الشركة أو الإسناد أو الدور عند إعادة التشغيل.
- [x] اختبارات بوابات إعداد الإنتاج وفشل الجاهزية الآمن وRate Limiting والإغلاق السليم.
- [x] عقد OpenAPI صالح آليًا من دون أخطاء أو تحذيرات وفق Redocly recommended.
- [x] اختبار MariaDB فعلي لسجل الأمان يشمل تدرج محاولات الدخول حتى القفل، المرشحات، الملخص، الإقرار، ومنع دور المحاسب.
- [x] اختبارات MariaDB فعلية للمصادقة والعزل والمستخدمين والفترات والحسابات والقيود وسندات القبض وسندات الصرف.
- [x] اختبارات التزامن للتسلسلات والترحيل وIdempotency وpost-vs-close وreverse-vs-settlement ومنع over-allocation للقبض والدفع على MariaDB وMySQL.
- [x] اختبارات توليد الرموز المرجعية المتزامن وثبات الرمز ومنع إدخاله أو تعديله يدويًا، واختبار دورة الاستعادة وإبطال الجلسات وإعادة استخدام الرمز.
- [x] اختبارات واجهة لمنطق المبالغ والتوزيعات والحالات وترجمة أخطاء الأعمال.
- [x] اختبارات حساب التدفق النقدي وصلاحيات التقارير وحد النطاق وتصدير ميزان المراجعة بصيغة CSV.
- [x] اختبارات المركز المالي وقائمة الدخل وتجميع شجرة الحسابات والمقارنة وCSV وXLSX وPDF، وكشف حساب الأستاذ والعميل والمورد وتصديره المدقق.
- [x] اختبارات دفتر اليومية للمرشحات والترقيم والصلاحيات وتصدير CSV الآمن وتسجيل التصدير.
- [x] تحقق وظيفي مباشر من تسجيل الدخول ومساري لوحة التحكم وميزان المراجعة على قاعدة XAMPP المحلية.
- [x] اختبار متصفح فعلي لتسجيل الدخول والتنقل والنماذج وRTL والتجاوب المكتبي والهاتف.
- [x] بوابة Chromium حتمية تغطي المستودعات والأرصدة والحركات ووحدات القياس والأصناف ومحرريها ونافذتي الفواتير بأربع لغات وأربعة عروض، وتعمل قبل E2E والإصدار.
- [x] بناء API للإنتاج.
- [x] بناء واجهة React/Vite للإنتاج.
- [x] تطبيق جميع الترحيلات الـ33 من الصفر محليًا على MariaDB 10.4.32، مع إثبات ترقية الترحيلات السابقة ببيانات تاريخية للذمم والضرائب ومرجعيات Treasury على MariaDB وMySQL؛ وأعاد تشغيل CI رقم 74 السلسلة الكاملة والنسخ والاستعادة على MariaDB 10.11.11 وMySQL 8.4.11 قبل النشر.
- [x] تشغيل API من `dist` باعتماديات الإنتاج فقط، مع نجاح Argon2 و`/live` و`/ready` وصفر تنبيهات اعتماديات تشغيلية.
- [x] نجح تشغيل GitHub Actions رقم 74 للالتزام `fa9a63665a91934b39d8d0af01383a92281ec5c3`: بوابتا MariaDB 10.11.11 وMySQL 8.4.11 وE2E والنسخ/الاستعادة والرجوع وإنشاء Artifact Linux ونشره والتحقق من الإنتاج كلها ناجحة. بصمة Artifact المسجلة: `sha256:76a695a9bafb83de382cd9536903401e96685db701a804163c1f81a010e5fcaf`.
- [x] تحقق تشغيلي مباشر في 24 أغسطس 2026: الصفحة العامة و`/live` و`/ready` أعادت `200`، وفحص الجاهزية أعاد قاعدة البيانات `ok` بزمن `8.29ms`، وحُمّلت واجهة الدخول دون أخطاء متصفح.
- [x] أضيف تحويل HTTPS آمن وثابت إلى مسار تفعيل CloudLinux مع استعادة `.htaccess` الأصلية عند الفشل، وحارس عام في وظيفة النشر يحفظ المسار والاستعلام؛ نجحت محليًا اختبارات البنية الـ19 والعقود والترجمة وعقد الواجهة وTypeScript وبناء API وVite، وتعيد CI اختبار ShellCheck وتكامل CloudLinux قبل التفعيل.
- [x] نجح تشغيل GitHub Actions رقم 75 للالتزام `d52b17e039a4d4d1a2cf467dedbe61d1ad8097d8`: اكتملت بوابتا قاعدة البيانات وE2E والنسخ والاستعادة وShellCheck والنشر، ثم أعاد HTTP العام `308` إلى المسار والاستعلام المطابقين وأعادت `/live` و`/ready` عبر HTTPS حالة `200` وقاعدة البيانات `ok`؛ بصمة Artifact `sha256:59029ca412559e44340fad3717731c1676f61c10106e874c2b6602ab62b6ef5d`.

## ضوابط معمارية مطبقة

- كل المبالغ المالية `DECIMAL` وتنتقل في API كنصوص.
- كل البيانات التشغيلية مقيدة بالشركة في الاستعلامات والعلاقات المركبة.
- المستندات تستخدم `DRAFT / POSTED / CANCELLED / REVERSED`.
- التعديل للمسودة فقط؛ التصحيح بعد الترحيل يتم بالعكس.
- العمليات الحساسة تستخدم CSRF وRBAC وOptimistic Version وIdempotency.
- الفجوات في تسلسل المستندات مقبولة ومدققة لأن الرقم يحجز عند إنشاء المسودة.
- البيانات الحساسة لا تعاد بصورتها الخام في استجابات API.

## أعمال متبقية معروفة

- [ ] إغلاق P0-2: تفعيل إنفاذ حماية `main` عبر GitHub Pro أو مؤسسة Team/Enterprise؛ اكتمل نقل أسرار `production` وإثباتها وحذف نسخ Repository والاعتماد المؤقت.
- [x] تفعيل نسخة مشفرة يومية خارج حساب الاستضافة وتجربة الاستعادة الشهرية؛ أُنشئ خط الأساس الإنتاجي واستُعيد فعليًا في تشغيل القبول `32840949844`، وتحقق منه المراقب في `32841244870`.
- [ ] إعادة اختبار بوابة التوافق دوريًا على أحدث إصدار تصحيحي مدعوم من MariaDB 10.11 ثم تحديث Digest المثبت لخدمة تمرين الاستعادة؛ التثبيت الحالي متعمد لمطابقة محرك الإنتاج الذي اجتاز المصفوفة، وليس بديلًا عن دورة ترقيع دورية.
- [ ] استكمال توثيق نافذة المراقبة والإطلاق التدريجي؛ التسجيل الذاتي واستعادة كلمة المرور ظاهران حاليًا في واجهة الإنتاج، ويجب ربط قرار استمرار تفعيلهما بمؤشرات التشغيل والـOutbox.
- [ ] بناء دورة أذونات استلام وتسليم مستقلة إذا احتاج التشغيل فصل الفاتورة عن الحركة، مع سياسة تمنع مضاعفة الأثر؛ تقييم المتوسط وربط تكلفة المبيعات بالفواتير مكتملان محليًا في المرحلة 62.
- [x] إضافة PDF تاريخي لفاتورة المبيعات، وتوسيع قوالب استيراد الفواتير بالصنف والمستودع مع مطابقة الرمز داخل الشركة والحفاظ على توافق القوالب السابقة؛ اكتمل محليًا في المرحلة 63.

## سياسة التوثيق المستمرة

عند إنهاء أي مرحلة لاحقة يجب، قبل الـcommit:

1. تحديث هذا الملف بقائمة ما نُفذ فعليًا.
2. تحديث `README.md` إذا تغير التشغيل أو الوحدات أو نتائج التحقق.
3. تسجيل اسم أي Migration جديد ونتيجة تطبيقه.
4. تحديث عدد الاختبارات ونتائج TypeScript والبناء.
5. تسجيل الأعمال المتبقية أو القيود المعروفة بصراحة.
