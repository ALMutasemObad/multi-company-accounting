# التعافي والنسخ الخارجية للبيئة المنشورة (Staging)

## الهدف والنطاق

تحمي هذه الخطة قاعدة Staging الحالية ووسائط المنتجات المرتبطة بها من فقدان حساب الاستضافة كاملًا، لا من فشل إصدار التطبيق فقط. أصبح `.github/workflows/production-backup-dr.yml` مسارًا يدويًا ينشئ عند الطلب نقطة استعادة مشفرة من عضو قاعدة بيانات وعضو وسائط، ويربط artifact وmanifest لكل عضو داخل pair manifest ذي بصمات، ثم ينقل الحزمة إلى GitHub Actions Artifact خارج حساب الاستضافة. يمكن للتمرين إعادة قاعدة البيانات إلى MariaDB مؤقتة والوسائط إلى مجلد مؤقت معزول. لا يكتب تمرين الاستعادة في Staging ولا يعيد Migration إلى الخلف. اسم الملف ومعرّفات `mcap-production-*` إرث تاريخي فقط ولا يغير تصنيف البيئة.

الحدود الحالية:

- حد دليل نقطة الاستعادة عند تشغيل المسار: **26 ساعة** من `recoveryPointAt`/`snapshotStartedAt`، ويستخدم فحص Artifact حدًا محافظًا **25.5 ساعة**. بعد إزالة الجدول الآلي لا يمثل هذا RPO مضمونًا أو SLO مفعلًا؛ عمر الاستعادة الفعلي يمتد حتى التشغيل اليدوي التالي.
- هدف تمرين الاستعادة التقني: إكمال فك تشفير قاعدة البيانات والوسائط، وإدخال SQL، واستخراج شجرة الوسائط الآمنة والتحقق منهما خلال **900 ثانية**. لا يساوي ذلك RTO الحادث الكامل من الإعلان إلى عودة التطبيق؛ يُقاس الأخير ويسجل أثناء الحادث أو تمرين خدمة كامل.
- لا توفر الاستضافة الحالية binlogs، لذلك لا توجد استعادة إلى نقطة زمنية بين النسخ المأخوذة يدويًا.
- عضوا البيانات مشفران، والـManifests والـpair manifest غير مشفرة لكنها منقحة وتحمل أسماء ملفات آمنة وبصمات وأحجامًا وحقول تحقق فقط؛ لا تحمل أسرار الاتصال أو اسم قاعدة المصدر أو أعداد الصفوف أو المجاميع المالية.

## المسار اليدوي الحالي

لا يحتوي Workflow على `schedule` أو `cron`. يشغله مسؤول من رأس `main` عبر `workflow_dispatch` مع خيار `restore_drill`:

1. يفحص عمر Artifact السابق قبل صنع النسخة الجديدة.
2. يرفض أي ref غير `refs/heads/main` ويتحقق أن `github.sha` هو رأس `main` الحالي قبل قراءة أي سر، ثم يثبت Actions وصورة MariaDB ببصمات immutable، ويثبت هوية SSH وبصمة المضيف ويرفع مساعدًا مؤقتًا إلى مسار صريح تحت `incoming`.
3. يمرر `BACKUP_ENCRYPTION_PASSPHRASE` و`MIGRATION_DATABASE_URL` عبر `stdin` من GitHub Environment ذات الاسم الإرثي `production`. الأسرار Environment-scoped وحُذفت نسخ Repository؛ الاسم نطاق وصول مؤقت ولا يعني أن الهدف Production، ولا يجوز إنشاء fallback أوسع.
4. يمسك قفل Staging البعيد نفسه، ويثبت أن حساب Passenger وحساب الترحيل يشيران إلى المضيف والمنفذ والقاعدة نفسها بصلاحياتهما المنفصلة، ثم يفحص سلامة المصدر. ينشئ أولًا نسخة SQL المشفرة ثم أرشيف `MEDIA_ROOT` المشفر؛ لأن ملفات الوسائط immutable ولا تُحذف في مسار الطلب، يبقى Snapshot قاعدة البيانات قابلًا للاستعادة حتى مع رفع صورة متزامن.
5. ينشئ pair manifest يربط artifact وmanifest لكل عضو ببصمة SHA-256، ثم ينزّل الملفات الخمسة إلى Runner ويعيد التحقق من الزوج ومن Manifest قاعدة البيانات مستقلًا قبل الرفع.
6. يرفع الحزمة باسم `mcap-production-backup-bundle` مدة 90 يومًا وبلا ضغط إضافي؛ Artifact الإصدار الرابع immutable داخل المحاولة. إذا أعيد التشغيل نفسه، يسمح `overwrite` فقط عندما تكون `github.run_attempt > 1` كي يستبدل Artifact المحاولة السابقة بدل الاصطدام باسمه، وتبقى كل تشغيلات dispatch المستقلة منفصلة.
7. بعد نجاح الرفع فقط، يحذف محليًا الحزم المكتملة المتحققة الأقدم من سبعة أيام كأزواج كاملة. يحتفظ بالحزم الناقصة للتحقيق وينظف ملفات `.partial` القديمة فقط؛ لا يمس نسخة نشر أخرى أو رابطًا رمزيًا.
8. عندما يكون `restore_drill=true` ينزّل Artifact الذي أُنشئ في التشغيل نفسه، يثبت أنه يحوي pair واحدًا وأعضاءه الأربعة فقط، ثم يعيد SQL إلى MariaDB 10.11.11 مؤقتة ويفك الوسائط إلى مجلد مؤقت غير موجود مسبقًا.
9. تتحقق أداتا الاستعادة من الجداول والترحيلات واتزان القاعدة، ومن مصادقة تشفير الوسائط وسلامة مسارات tar وأن الناتج ملفات ومجلدات عادية بلا روابط. ثم تحفظان دليلًا منقحًا باسم `mcap-production-restore-drill` يتضمن RPO/RTO والبصمات والنتيجة.

يشترك هذا المسار ووظيفة نشر Staging في ملف القفل البعيد `.pipeline.lock` وينتظر كل منهما حتى 900 ثانية قبل الفشل الصريح؛ فلا يبدأ `mysqldump` أثناء تطبيق Prisma Migration ولا يبدأ نشر جديد أثناء أخذ النسخة. لا تستخدم الوظائف مجموعة GitHub `concurrency` مشتركة، لأن GitHub قد يستبدل الطلب المنتظر الأقدم بطلب أحدث حتى مع تعطيل الإلغاء؛ القفل البعيد هو مصدر التسلسل الحاسم ولا يسقط الطلبات بصمت.

أصبح `production-metrics-monitor.yml` يدويًا أيضًا. عند تشغيله يفحص Artifacts المربوطة حصريًا بتشغيلات `production-backup-dr.yml` على `main`: يفشل عند غياب النسخة أو تجاوز Artifact 25.5 ساعة، وعند غياب دليل استعادة ناجح أو تجاوزه 35 يومًا، ويحافظ على علامة أي تمرين مطلوب فاشل أحدث من آخر نجاح. هذه بوابة دليل لحظة التشغيل وليست مراقبة مستمرة؛ غياب تشغيلها لا يثبت السلامة، ويلزم مراقب مستقل مجدول قبل Production الحقيقي.

## تشغيل القبول أو التمرين يدويًا

يتطلب المسار `CPANEL_SSH_PRIVATE_KEY` و`MIGRATION_DATABASE_URL` و`BACKUP_ENCRYPTION_PASSPHRASE`، ويتطلب فحص المراقبة `METRICS_BEARER_TOKEN`. توجد القيم مؤقتًا كـEnvironment secrets داخل Environment ذات الاسم الإرثي `production`، بعد حذف Repository copies. لا تنقلها إلى Repository variables/secrets أو إلى وسيطة أمر. خطة النقل هي إعادة إدخالها من مدير الأسرار الخارجي إلى Environment باسم `staging`، ثم إثبات نشر ونسخة/استعادة ومراقبة يدوية ناجحة قبل حذف القيم القديمة؛ لا يمكن ولا ينبغي استخراج القيم المخفية من GitHub لنقلها.

من واجهة GitHub Actions اختر `staging backup and restore drill (manual)` ثم `Run workflow` واجعل `restore_drill=true`. ومن GitHub CLI بعد وجود Workflow على الفرع الافتراضي:

```bash
gh workflow run production-backup-dr.yml --ref main -f restore_drill=true
```

لا تعد القناة سليمة حتى ينجح ما يلي في التشغيل نفسه:

- `Create encrypted offsite staging backup manually`.
- `Restore the staging backup into isolated MariaDB`.
- `Record staging recovery evidence`.
- وجود Artifact النسخة وArtifact الدليل `mcap-production-restore-drill`، وداخله الملف المنقح `dr-evidence.json`، ببصمة ومدة احتفاظ ظاهرتين.
- تشغيل مراقبة Staging يدويًا بعد ذلك من دون تنبيه missing/stale.

قد تكون حالة الدليل `passed_with_recovery_gap` عندما تكون النسخة السابقة أقدم من حد القياس. هذه الحالة تثبت نجاح النسخة والاستعادة المطلوبتين الآن، لكنها تسجل صراحة أن الاستمرارية لم تحقق حد 26 ساعة ولا تمنح ادعاء RPO؛ لذلك تظهر Warning بدل إفشال تمرين Staging تقني سليم.

### دليل القبول التاريخي للهدف المنشور

في 25 أغسطس 2026 نشر تشغيل [32839845689](https://github.com/ALMutasemObad/multi-company-accounting/actions/runs/32839845689) الالتزام `c39b97949655f43c9fe35f98b3d47cce5c6c6054` إلى الهدف الذي كان موصوفًا آنذاك بالإنتاج ثم أعيد تصنيفه Staging. أنشأ تشغيل القبول [32840949844](https://github.com/ALMutasemObad/multi-company-accounting/actions/runs/32840949844) نسخة مشفرة واستعاد Artifact نفسه إلى MariaDB 10.11.11 معزولة؛ كان عمر نقطة الاستعادة 7 ثوانٍ، واستُعيدت 56 جدولًا و38 Migration خلال ثانية واحدة مقابل حد 900 ثانية. نجح فحص [32841244870](https://github.com/ALMutasemObad/multi-company-accounting/actions/runs/32841244870) بعد ذلك دون تنبيه. هذه أدلة تاريخية على سلامة الأدوات في ذلك التشغيل، وليست إثباتًا لنسخ دوري أو RPO/SLO حالي بعد تعطيل الجداول.

## مصفوفة الفشل والاستجابة

| الفشل | السلوك الآمن | الإجراء |
|---|---|---|
| تعذر SSH أو النسخ | لا يرفع Artifact جديدًا ويفشل التشغيل | افحص حالة iFastNet وبصمة المضيف والمفتاح، ولا توسع الصلاحيات قبل تحديد السبب |
| غياب/تقادم النسخة السابقة | تستمر محاولة صنع نسخة جديدة، لكن بوابة RPO تفشل بعد الحفظ والتمرين | تعامل معه كخرق RPO وسجل مدة الفجوة وسبب التشغيل المفقود |
| اختلاف الحجم أو SHA-256 أو Manifest | يرفض الملف قبل الرفع أو قبل فك التشفير | لا تحاول إصلاح Manifest يدويًا؛ أنشئ نسخة جديدة وحقق في التخزين/النقل |
| عبارة تشفير خاطئة أو ملف مبتور | تفشل مصادقة AES-GCM ولا تدخل بيانات جزئية معتمدة | استرجع إصدار المفتاح الصحيح من مدير الأسرار واستخدم Artifact سابقًا سليمًا |
| قاعدة الاستعادة غير فارغة | ترفض الأداة البدء | أنشئ قاعدة مؤقتة جديدة؛ لا تستخدم `--force` ولا تنظف هدفًا غير محسوم |
| غياب بيانات الهوية الأساسية أو اختلال دفتر المصدر قبل النسخ | لا ينشأ ملف جديد ولا يُرفع Artifact | تحقق من اختيار قاعدة Staging ومن سلامة المؤسسة والشركة والمستخدم والقيود قبل إعادة المحاولة |
| فشل الترحيلات أو الجداول أو اتزان القاعدة المستعادة | يفشل التمرين وتُهدم خدمة Runner المؤقتة | صنف النسخة غير مقبولة وابحث عن اتساق dump أو تغير الأداة قبل اعتمادها |
| تجاوز 900 ثانية | يحفظ دليل فشل منفصل ويفشل هدف الاستعادة التقني | قس فك التشفير والإدخال والتحقق منفصلة، وراجع حجم القاعدة وسعة الهدف؛ لا تخلط القياس مع RTO الحادث الكامل |
| تأخر التشغيل اليدوي | لا توجد آلية تكتشفه تلقائيًا في الحالة الحالية | افحص عمر آخر Artifact قبل أي اعتماد، وشغّل نسخة/استعادة يدويًا؛ لا تدّع RPO مضمونًا |
| توقف GitHub Actions أو نفاد الحصة | يتعذر النسخ والفحص اليدويان | افحص Heartbeat خارجيًا/يدويًا وحالة Actions، ولا تعتبر غياب التنبيه إثبات سلامة |

## استعادة الخدمة بعد فقدان الاستضافة

1. أوقف الكتابة إلى البيئة المتضررة وسجل وقت آخر عملية موثوقة؛ لا توجه تطبيقين قابلين للكتابة إلى القاعدة نفسها.
2. اختر أحدث `mcap-production-backup-bundle` من تشغيل ناجح. فضّل تشغيلًا يملك أيضًا دليل استعادة ناجحًا، لكن أعد التحقق من النسخة المختارة دائمًا.
3. نزّل ملفات الحزمة الخمسة إلى محطة استجابة مشفرة ومقيدة. احصل على إصدار عبارة التشفير الموافق من مدير أسرار مستقل عن iFastNet.
4. شغّل `verifyBackupPair` من `scripts/lib/backup-pair-verifier.mjs` ثم `scripts/verify-backup-artifact.mjs` على عضو SQL قبل فك التشفير، وأنشئ قاعدة MariaDB/MySQL فارغة متوافقة وهوية استعادة مؤقتة عليها فقط.
5. اضبط `DATABASE_URL` و`BACKUP_FILE` و`BACKUP_ENCRYPTION_PASSPHRASE` و`RESTORE_CONFIRM=RESTORE:<database>`، ثم شغّل `node scripts/database-restore.mjs`. استعد عضو الوسائط عبر `node scripts/media-restore.mjs <media.jwb> <new-media-directory>` إلى مسار جديد، وبعد نجاح التحقق انقل الخدمة إلى القاعدة والمجلد المتوافقين في نافذة صيانة واحدة.
6. لا تعتبر نجاح استيراد SQL كافيًا؛ يجب نجاح فحص الجداول والترحيلات واستعلام تعدادات الجداول المرجعية ووجود بيانات الهوية الأساسية واتزان المدين والدائن. لا يقارن v2 التعدادات بعدد مصدر منشور، لذلك أكمل تحقق الحادث الوظيفي في الخطوة التالية. افحص حالة Prisma من Checkout مطابق للالتزام بعد `npm ci`، أو من محطة الاستجابة المعزولة عبر `npx --yes prisma@7.9.1 migrate status` داخل `apps/api`؛ أداة Prisma CLI محذوفة عمدًا من Release التشغيل المنقّى.
7. جهز حساب تشغيل DML فقط وحساب ترحيل مستقلًا، وشغّل الإصدار الموافق في عنوان معزول، ثم اختبر `/live` و`/ready` والدخول وتقارير ميزان المراجعة قبل تحويل DNS/Passenger.
8. وثق RPO الفعلي بدءًا من `recoveryPointAt` (وهو `snapshotStartedAt` في v2؛ يستخدم v1/التحويل القديم `createdAt` كبديل محدود الدقة) إلى آخر كتابة مفقودة، ووثق RTO الحادث من الإعلان إلى عودة الخدمة، وأبق البيئة الأصلية للطب الشرعي ما لم يشكل بقاؤها خطرًا.

استعادة شركة واحدة لا تدخل مباشرة فوق الإنتاج. تستعاد النسخة كاملة في بيئة معزولة، ثم يراجع استخراج الشركة وعلاقاتها وميزانها بخطة منفصلة.

## الاحتفاظ وتدوير المفتاح

- تحتفظ GitHub بكل نسخة يدوية وأدلة تمرينها 90 يومًا. تبقى Artifacts منفصلة حسب Run؛ لا يسمح المسار بالاستبدال في المحاولة الأولى، ويستخدم `overwrite` المقيد بمحاولة إعادة التشغيل فقط لمعالجة الاسم الموجود من المحاولة السابقة. سجل `run_attempt` عند التحقيق لأن Artifact المعاد رفعه يملك هوية وبصمة إنشاء جديدتين.
- يحتفظ Workflow بسبعة أيام محليًا من نسخ `mcap-production-*` ويحذف الأقدم بعد نجاح رفع النسخة الخارجية فقط، وينظف مخلفات النمط نفسه بعد مهلة يوم. لا يطابق هذا التنظيف نسخ ما قبل النشر ذات الأسماء الأخرى؛ تبقى سياسة مساحتها إجراءً مستقلًا حتى لا يحذف المسار الآلي أدلة رجوع لم توضع في نطاقه.
- تدوير `BACKUP_ENCRYPTION_PASSPHRASE` لا يعيد تشفير Artifacts القديمة. احتفظ بالمفتاح السابق، موسومًا بمدة صلاحيته، في مدير أسرار مستقل حتى انتهاء آخر Artifact يعتمد عليه.
- بعد كل تدوير شغّل نسخة وتمرين استعادة يدويين بالمفتاح الجديد قبل إيقافه عن الاستخدام، واختبر Artifact قديمًا بالمفتاح السابق في محطة معزولة.

## نموذج التهديد والحوكمة

يفصل هذا التصميم النسخة عن حساب iFastNet ويحمي المحتوى بتشفير مستقل. لكنه لا يفصل Artifact ومفتاح التشغيل عن سيطرة GitHub بالكامل: يستطيع مهاجم يملك تعديل Workflow منفذًا على `main` إساءة استخدام أسرار البيئة. لذلك يبقى إنفاذ حماية `main` عبر GitHub Pro أو مؤسسة Team/Enterprise ضابط P0 مستقلًا، ويجب حفظ نسخة من مفتاح التشفير في مدير أسرار خارج GitHub وiFastNet معًا.
