# ربط اكتشاف الخطط — دفعة لاحقة مستقلة

التاريخ2026-08-31؛ النسخة `D:/CodexWorktrees/grocery-next-wave-coordinator`،
الفرع `feat/grocery-next-wave-coordination`. أصل الإصدار المدمج89ba49b منPR45.
هذه التغييرات **محلية وغير منشورة**؛ لا تدخل تشغيل نشر PR45 الجاري.

## التنفيذ الفعلي

- استورد S1: d590f97/d501859/ba25c4d إلى8479a68/348dda9/e89c2b4؛
  واستورد S2:1f0582c/8d5644a إلى0b4a102/c8a58b7. لا استيراد N1/N2/N3/Z1.
- ربط رابط الخطة لصاحب الحساب بمصنع URL واحد، ومشاركة التحقق من المعرّف النصي
  بين login/register/subscription. لا plan من مسار مشابه أو reset-password.
  المعامل المكرر أو الفارغ/غير الصالح لا يحيي اختيارًا أقدم من التخزين.
- يحمل URL اختيار المستخدم عند تعطيل sessionStorage وعند فتح تبويب جديد؛
  تتحرك النية بين الدخول والتسجيل والمراجعة ولا تنتقل token/redirect/activate.
  يعمل الرجوع والتقدم بين الدخول والتسجيل، ولا يعاد POST من التنقل.
- يبقى التفضيل غير موثوق؛ معرّف موجود في الرابط لا يحجز سعرًا أو يمنح صلاحية.
  الكتالوج المصادق يعيد التحقق. الغائب يبقى placeholder مع رسالة وزر مراجعة
  معطل؛ لا استبدال بأول خطة أو phantom option. تزال نية الرابط بعد إقرار تغيير
  الاشتراك الفعلي، لا عند بدء الطلب أو النتيجة المجهولة.
- ربط شريط S1 في الرئيسية فقط، لا الكاشير. قراءة واحدة محدودة للمالك المخول
  `GET /subscription?page=1&pageSize=1` بمهلة10ثوان وإلغاء ودون retry.
  تبديل المستخدم/النشاط/الصلاحيات/الموديولات يعيد تركيب الجسم ويلغي القراءة؛
  لا يعرض ردًا من نطاق سابق. الموظف يرى إرشادًا عامًا بلا قراءة بيانات تجارية.
  الإغلاق محفوظ في ذاكرة نسخة App للمستخدم والنشاط خلال التنقل، ويعاد المخزن
  عند الخروج/تبديل المستخدم. إعادة تحميل App ليست احتفاظًا دائمًا بالإغلاق.
- زر الشريط يفتح كتالوج الحساب/مراجعة الاشتراك الموجودين؛ لا فتح صفحة كاملة
  يمحو ذاكرة محاولة POS ولا أمر اشتراك أو فوترة. أخطاء القراءة لا تُعرض كغياب
  اشتراك. لا ترتيب ترقية مستنتج؛ relationships غير متاحة صراحة في هذا الربط.
- `SubscriptionUpgradeCard` ما زالت مكوّنًا جاهزًا غير مركب؛ لم نكرر عرض الخطة
  الحالية داخل صفحة الحساب. جرى تركيب Banner فقط.

الاختيار في URL يستمر مع الرابط، أما التخزين المساعد فله24ساعة. رابط بريد جديد
لا يحمل الاختيار لا يُنشئ ذاكرة مشتركة بين التبويبات؛ قد يلزم إعادة الاختيار.
لم يتغير بريد التسجيل أو حمولة تسجيل/دخول أو CSRF أو الصلاحيات أو أي API/schema.

## الدلالات التجارية التي لم تُخترع

تبقى غيابات صف الاشتراك أخطاء لا confirmed-absent، وتبقى Legacy/full-access
اشتراكًا قائمًا لا FREE/BASIC/TRIAL. API الحالي لا يقدم ترتيب ترقية موثوقًا؛
لذلك المدخل محايد إلى الخطط والمراجعة، لا ادعاء «أعلى» من الاسم أو السعر.
فصل تجهيز النشاط الجديد عن grandfathering، واعتماد الأسعار والخطة الابتدائية
والحصص وعلاقة الترقية، مهام مالك الاشتراكات قبل استقبال عميل مدفوع.
لا إعلان تلقائي لأي إصدار؛ publiclyListed=false يبقى قرار حماية صحيحًا.

## الأدلة المحلية وحدودها

138/138 وحدة في8ملفات بعد الربط؛ TypeScript Web وملفات E2E،i18n/UI27/67 وبناء
Web162modules نجحت. اختبارات الربط الأولى8/8 للروابط بأربع لغات و390px، ثم
2/2 للشريط بعد تركيبه. ثم اجتازت النسخة النهائية **10/10 في تشغيل واحد**،51.3ث،
عامل واحد وبلاretries، مع إعادة138/138وحدة والأنواع والبناء وحراس الواجهة قبلها.
دليلها `tmp/subscription-discovery/final-browser/.last-run.json`. توقفت خوادم
الاختبار بعد الجولة؛ لم يبدأ فحص شامل آخر بسبب المساحة الحرجة علىC.

الفحص يستخدم التطبيق الفعلي مع HTTP fixtures محلية على3163/4213، لا تسجيل
حقيقي أو بريد أو دفع أو كتالوج تجاري معتمد. لا تنسب أدلة S1/S2 السابقة إلى
شيفرة الربط الجديدة. يلزم قبول بصري RTL/LTR بمقاسات السياسة، واختبارات تغير
الشركة/التفويض والهوية المتأخرة عبر الواجهة، وفحص شامل وعقود وبوابات المحركين
قبل PR/دمج مستقل. لا تورث موافقة PR45 إلى هذا الفرع تلقائيًا.

الأوامر: `subscription-discovery-vitest.config.mjs`،
`tsconfig.subscription-discovery.json`،`vite.subscription-discovery.config.mjs`،
`playwright.subscription-discovery.config.ts`. جميع TEMP/cache/build/results
تحت `tmp/subscription-discovery` فيD،عامل واحد،دون تثبيت/توليد Prisma.

## Barcode Impact

لا صنف/باركود أو طباعة أو دفتر أو سعر محسوب. لا تغيير scanner/codec أو أي مالك
كتابة. الشريط محصور بالرئيسية ولا يحجب الكاشير. Z1 منفصل بلا تركيب أو جسر أو
ادعاء جهاز؛ تسليمه823d905/b1e1d20/c52cf1b مع161اختبارًا مركزًا وقيوده فيhandoff.
