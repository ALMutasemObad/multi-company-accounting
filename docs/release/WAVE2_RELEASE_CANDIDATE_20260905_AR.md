# مرشح تكامل Wave 2 — 5 سبتمبر 2026

## الحالة والنطاق

هذا مرشح محلي غير منشور على الفرع
`integration/wave2-release-candidate-20260905` فوق Staging
`4c254b3abe934821ad323ec41a7451a7341bc963`. دُمجت رؤوس المالكين التالية فقط:

- CRM: `b05808d`.
- Organization: `3b772e36d29c2d0ace952e9cbaf37dbaf03f76c9`.
- Employee Expenses: `a9999871a22fea56ae5f9c36977f7a78c4ada873`.

كانت الرؤوس الثلاثة مبنية على الشجرة المشتركة `12f5e074`، وهي مطابقة لشجرة خط
Staging الحالي؛ لذلك أُخذ commit الشريحة النهائية لكل مالك ولم تُكرر السلسلة المشتركة.
لم يحدث Push أو PR أو Deploy.

## حدود الملكية المثبتة

- CRM ينشئ/يقرأ العميل من خلال منافذ Sales ولا يكتب `Customer` مباشرة، ولا ينشئ
  مشروعًا أو قضية قبل اكتمال فحص تعارض المصالح F2.
- أدوار Organization (`OWNER/ADMIN/VIEWER`) مستقلة عن صلاحيات الشركة وعن صلاحيات
  مشغل المنصة. تعرض اللوحة مؤشرات كل شركة بعملة أساسها ولا تجمع قوائم مالية موحدة.
- Employee Expenses يملك المطالبة فقط وينتهي عند `READY_FOR_PAYMENT`; لا ينشئ
  `Payment` أو ذمة أو مستندًا محاسبيًا أو أثر Ledger.

## العقد والترحيلات

- OpenAPI الموحد: `1.48.0`، بعدد 349 عملية و181 جسم طلب و2318 جسم استجابة.
- أعيد توليد `openapi-request-guards.ts` من `packages/contracts/openapi.yaml`، ونجح
  فحص التقادم والتغطية.
- ترتيب ترحيلات Wave 2: Organization رقم 68
  (`20260904120000_organization_membership_owner_workspace`)، ثم Employee Expenses
  رقم 69 (`20260904150000_employee_expense_claims`)، ثم CRM رقم 70
  (`20260904193000_crm_business_development_vertical`).

## تحقق المرشح المحلي

| البوابة | النتيجة |
|---|---|
| Prisma validate / generate | ناجح على المخطط الموحد |
| TypeScript: API source/tests وWeb وE2E | ناجح |
| Build: API وWeb | ناجح؛ يوجد تحذير حجم chunk موضح أدناه |
| Web unit tests | 654/654 ناجحة في 46 ملفًا |
| API unit/architecture tests | 1229 ناجحة، 161 متجاوزة بشروط DB، في 126 ملفًا ناجحًا و40 ملفًا متجاوزًا |
| اختبارات الوحدات الثلاث المركزة | 28 ناجحة و4 متجاوزة بشروط DB؛ 10 ملفات ناجحة وملفان متجاوزان |
| Infra | 324 ناجحة، وحالة Linux-native واحدة متجاوزة، بلا فشل |
| OpenAPI guards / route inventory | ناجح |
| Redocly 2.46.2 | ناجح مع تحذير واحد لمكوّن قديم غير مستخدم |
| i18n / UI contract | ناجح؛ 31 عنوان صفحة و68 منطقة جدول |
| Playwright CRM / Organization / Employee / الرحلة الموحدة | ناجح عند 390 و1440 |

رحلة العرض العربية تبدأ من `#home` وتفتح مساحة مالك المجموعة وCRM ومصروفات
الموظفين ثم تعود إلى الرئيسية بين الوحدات. أدلة اللغات `ar/en/ur/hi` محفوظة في
`docs/visual-qa` بأسماء `crm-*` و`organization-owner-*` و`employee-expenses-*`،
وأدلة الرحلة الموحدة باسم `wave2-release-candidate-ar-*`.

## بوابة قاعدة البيانات المطلوبة في CI

لا تتوفر قاعدة بيانات محلية لهذا الـworktree، لذلك لم تُشغّل الحالات المشروطة. قبل
الدمج أو أي نشر يجب أن تنجح على MariaDB 10.11 وMySQL 8.4 لكل محرك:

1. تطبيق جميع الترحيلات على قاعدة فارغة ثم Seed واختبارات التكامل.
2. ترقية نسخة ممثلة لخط Staging عبر الترحيلات 68 ثم 69 ثم 70، مع فحص العزل والـbackfill.
3. اختبار rollback المحروس بالترتيب العكسي، وإثبات رفضه عند وجود سجل أعمال أو تدقيق.

## مخاطر التسليم المفتوحة

- بوابة fresh/upgrade/rollback واختبارات DB ما زالت مطلوبة في CI، وهي مانع إصدار حتى
  نجاحها على المحركين.
- يبني Vite بنجاح لكنه يحذر من chunk باسم `employee-expenses` حجمه نحو 539 kB قبل
  الضغط؛ يوصى بتتبعه كتحسين أداء قبل الإنتاج إن لم تخفضه سياسة التقسيم النهائية.
- Redocly يحذر من المكوّن القديم غير المستخدم `PosCheckoutRecoveryResult`; لا يؤثر
  في صحة العقد لكنه دين تنظيف غير مرتبط بـWave 2.
- اللقطات تستخدم خادم Visual QA وfixtures حتمية؛ لا تستبدل رحلة بيئة متصلة بقاعدة
  البيانات بعد نجاح بوابة CI.

هذا التقرير لا يصرح بالنشر، ولم يُنشأ Push أو PR أو Deploy.
