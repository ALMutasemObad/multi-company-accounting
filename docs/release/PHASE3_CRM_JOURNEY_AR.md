# تسليم رحلة العملاء — المرحلة الثالثة

التاريخ: 2026-09-06. المهمة `crm-journey`، الفرع `task/crm-journey`، الأساس المتحقق منه `9be397c7a435720c921425b6cb7b64e5dbab7ebc`.
هذا تسليم محلي للمراجعة؛ لا push أو PR أو CI بعيد أو دمج أو نشر لهذه الحزمة. الالتزام النهائي يسجله مدير مساحة العمل عبر `MarkReady` بعد الحفظ، ويمكن تحديده بـ `git log -1` على الفرع.

## ما نُفذ

- إعادة إنشاء مساحة CRM الداخلية عند تغير المستخدم أو الشركة أو الصلاحيات الفعلية. لا تنتقل النتائج أو صفحات البحث أو النماذج إلى الهوية الجديدة.
- إلغاء القراءة السابقة عند تغيير البحث/الصفحة/اللغة، والتحقق من هوية الطلب قبل تطبيق النتائج والأخطاء وحالة التحميل. تنظيف القراءة والكتابة عند unmount في layout effect، دون الاعتماد على أي تعديل في App أو api.
- قفل متزامن قبل أول انتظار للحفظ، مع تعطيل إجراءات السجلات والحقول والتصفح أثناء الكتابة، ومنع إغلاق النموذج أثناء الطلب. تُفحص صلاحية الأمر في منفذه أيضًا.
- حفظ المدخلات عند فشل الطلب، وعدم عرض نجاح أو إعادة تحميل من طلب تابع لشركة سابقة. بعد نجاح الحفظ، يعاد تحميل العرض الحالي؛ فشل هذا التحديث يظهر خطأ وإعادة محاولة دون إعادة إرسال أمر الكتابة.
- الاحتفاظ بمفتاح Idempotency لإعادة محاولة نفس المسار ونفس الجسم الفاشل داخل مساحة الشركة المركبة. الجسم المعدل أو الأمر الجديد بعد نجاح مؤكد يحصل على مفتاح جديد.
- إصلاح مانع إنشاء العميل ذي الاسم العربي: كان الاسم يدخل مباشرة في ترويسة Idempotency-Key ويفشل تحويل HTTP ByteString قبل الإرسال. المفتاح الآن تقني ASCII ولا يتضمن اسم العميل.
- عرض estimatedAmount وweightedAmount باستخدام `formatCurrencyDecimal` وintlLocale مباشرة من السلاسل العشرية. لا تمر الأموال عبر Number، وتبقى دقة الأربع خانات القادمة من العقد مع مراعاة منازل العملة. الاحتمال والعدادات غير المالية تبقى أعدادًا.
- توضيح نطاق البحث (العملاء والفرص فقط)، وحالات التحميل والحفظ وعدم التطابق، والخطوة التالية لكل حالة عميل محتمل. ترجمة الإضافات للعربية والإنجليزية والأردية والهندية في قاموس CRM القائم.
- اشتراط اختيار العملة عند إدخال مبلغ في التأهيل وفق شرط العقد الموجود، وإضافة نوع الهاتف ولوحة إدخاله، وتحسين مسافات النموذج وعدم التفاف تسمية المرحلة على الهاتف. مقاسان أساسيان للخط.

لا تغيير للمجال أو البيانات أو العقود أو الصلاحيات الخادمية؛ لا إنشاء قضايا ولا تنفيذ فحص تعارضات. التحويل الموجود إلى عميل المبيعات فقط. جميع الملفات المعدلة ضمن النطاق المسند.

## الأدوات وإعادة التشغيل

تم تشغيل `Enter-Workspace.ps1` و`Workspace.ps1 -Action Check -Refresh`: نجاح main=9be397c، active=3/3. اصطدمت محاولتان أوليتان بقفل السجل أثناء فحص متزامن؛ أعيد الفحص دون حذف القفل.

Node المستخدم: `C:/Users/motas/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node.exe`، النسخة `v24.19.0`.
لم يكن npm موجودًا في runtime. بموافقة المدير جُلبت metadata من `https://registry.npmjs.org/npm/12.0.2` وحزمة dist المشار إليها، وقورنت SHA-512 بصيغة Base64 مع `dist.integrity` قبل فكها إلى `.tools/npm-bootstrap` المتجاهل داخل المهمة. تحقق `--version` أعاد `12.0.2`.
ثبت `npm ci --no-audit --no-fund` اعتماد المهمة المستقل من package-lock: نجاح، 359 حزمة. لا global ولا نسخ أو junction لاعتمادات أخرى ولا تعديل lock.

من جذر worktree، بعد إضافة مسار Node السابق إلى PATH لهذه الجلسة فقط:

```powershell
node .tools/npm-bootstrap/package/bin/npm-cli.js ci --no-audit --no-fund
node .tools/npm-bootstrap/package/bin/npm-cli.js run typecheck -w @mcap/web
node .tools/npm-bootstrap/package/bin/npm-cli.js run build -w @mcap/web
node .tools/npm-bootstrap/package/bin/npm-cli.js run i18n:check
node .tools/npm-bootstrap/package/bin/npm-cli.js run ui:check
node .tools/npm-bootstrap/package/bin/npm-cli.js run test -w @mcap/web -- src/crm-journey/command-sender.test.ts src/decimal-format.test.ts src/authorization.test.ts src/i18n/i18n.test.ts
node node_modules/@playwright/test/cli.js test --config apps/web/src/crm-journey/playwright.config.mjs
git diff --check
```

## ما اختُبر والنتائج

- TypeScript وbuild الويب: نجاح. تحذير Vite عن chunk أكبر من 500KB موجود في employee-expenses؛ ليس فشل بناء ولا جزءًا من نطاق CRM. ملف CRM الناتج نحو 20KB.
- i18n:check وui:check: نجاح.
- 29 اختبارًا في أربعة ملفات Vitest: نجاح؛ تشمل دقة المبالغ والصلاحيات والقواميس وثلاث حالات لمفاتيح إعادة المحاولة (نفس الجسم، جسم/شركة مختلفان، أمر جديد بعد نجاح).
- 13 اختبار Playwright Chromium بواجهة React الفعلية وStrictMode: نجاح. يشمل الاسم العربي، تكرار requestSubmit المتزامن وإرسال واحد، 403 وحفظ المدخلات وإعادة المحاولة بالمفتاح نفسه، تأهيل بمبلغ كبير دون فقد الدقة، اشتراط العملة، التحويل إلى عميل قائم مع version، وإكمال المتابعة.
- اختبارات المتصفح تغطي قارئًا دون أزرار كتابة، وصلاحية متابعة فقط وصلاحية تحويل فقط، وسباق بحثين، وفشل القراءة وإعادة المحاولة، وتبديل الشركة أثناء القراءة والكتابة، ومسح مسودة الشركة السابقة، وunmount أثناء القراءة والكتابة. النقل التجريبي يتعمد تجاهل AbortSignal وإرجاع النتائج المتأخرة.
- تحقق عرض 390×844 وعدم تجاوز العرض الأفقي، وفحص بصري لصورة نموذج الهاتف بعد إنهاء animation. الدليل المحلي المتجاهل `test-results/crm-journey/mobile-form.png`؛ لم يضف إلى Git. بقية اختبارات المتصفح تعمل بقياس سطح المكتب الافتراضي.
- تمت مراجعة diff وفحص whitespace. الاختبارات كشفت وأصلحت عطل اسم العميل العربي؛ أُصلح أيضًا إعداد fixture ليشمل SALES لأن ربط صلاحيات CRM الحالي يتطلبها. فشل أولي لاختبار الوحدة كان بسبب hook اختبار يعيد mock كدالة cleanup؛ صُحح ثم نجحت الحالات الثلاث.

## ما لم يُختبر والمتبقي للمراجعة

- لا قاعدة بيانات حقيقية ولا قبول خادمي ولا CSRF فعلي ولا جلسة حية في هذه الجولة. الـAPI وواجهات React الفعلية مستخدمة فوق fetch تجريبي؛ لا يدعي ذلك سلامة المعاملة أو تنفيذ RBAC خادميًا.
- لم تختبر الأجهزة الفعلية أو Safari/Firefox، ولم يجر تدقيق بصري كامل لكل اللغات.
- لا استرجاع دائم لمسودة أو محاولة بعد إغلاق الصفحة أو تغيير الشركة/الصلاحيات. إلغاء الطلب محليًا يمنع تأثير نتيجته على الواجهة ولا يثبت إلغاء معاملة وصلت للخادم. يجب مراجعة سجلات الشركة الأصلية عند العودة إليها؛ لا إعادة تلقائية للكتابة.
- إعادة المحاولة بنفس المفتاح تخص المدخلات غير المعدلة والمساحة الحالية. تعديل المدخلات بعد نتيجة شبكة مجهولة ينشئ أمرًا جديدًا؛ لا توجد هنا منظومة استرداد دائمة لنتائج الكتابة المجهولة.
- بعد دمج `session-safety` محليًا بواسطة المدير: إعادة اختبارات المتصفح المذكورة، وإضافة تحقق متكامل أن INVALID_CSRF يحافظ على نموذج CRM، وأن 401 الحقيقي يغلق shell مع تنبيه فقد المدخلات، ثم دخول جديد وتبديل الشركة. لم يُنسخ أو cherry-pick التزام مسؤول الجلسة إلى هذا الفرع.
- واجهة اختبار `browser.html` وfixture والتهيئة داخل `crm-journey` ليست مستوردة من entry التطبيق ولا تدخل build الإنتاج. الواجهة المحلية تستمع على 127.0.0.1:4197 أثناء الاختبارات فقط.
- لا تعارض كتابة معروف مع المهمتين الأخريين؛ لم تتغير App.tsx/api.ts أو توقيعاتهما ولا سجل الترجمة أو الاعتماد. أي مراجعة أو دمج أو نشر بعيد يتطلب إذن المستخدم المناسب.
